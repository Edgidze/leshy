# Релизная подготовка iOS — инструкция для агента

Путь в репозитории: `docs/release/ios-app-store-setup.md`
Парный документ по Android — `docs/release/android-release-setup.md`.

Цель — довести проект до состояния, когда подписанный архив принимается App Store Connect,
раздаётся тестировщикам через TestFlight и проходит ревью в App Store.

Задачи выполнять **строго по порядку**, каждая — отдельная сессия агента (`/clear` между
задачами). Этот файл в контекст автоматически не подтягивается: ссылайтесь на него явно,
например «выполни задачу 3 из `docs/release/ios-app-store-setup.md`».

У каждой задачи указан исполнитель: **владелец** (действия в консолях Apple и в Xcode, агент
туда не ходит) или **агент** (правки в репозитории).

---

## Состояние на момент составления (2026-09-12, проверено по коду)

- Android-версия уже в закрытом тестировании Google Play, `versionCode=4`, `versionName=1.0`
- платная подписка Apple Developer Program оформлена, `DEVELOPMENT_TEAM = 78XLSLLP9C`
  прописан в `project.pbxproj`
- `PRODUCT_BUNDLE_IDENTIFIER = leshy.mushrooms.map` совпадает с `applicationId` Android
- `MARKETING_VERSION = 1.0`, `CURRENT_PROJECT_VERSION = 1` (`iosApp/Configuration/Config.xcconfig`)
- `IPHONEOS_DEPLOYMENT_TARGET = 15.0`, `TARGETED_DEVICE_FAMILY = "1,2"`, `ARCHS = arm64`
- иконка 1024×1024 без альфа-канала лежит в asset catalog — отдельной загрузки не требует
- `NSCameraUsageDescription` и `NSLocationWhenInUseUsageDescription` на месте; галерея сделана
  через `PHPickerViewController`, отдельная usage-строка ей не нужна
- политика конфиденциальности опубликована и общая с Play:
  `https://leshy-mapper.github.io/mushrooms-map/privacy.html`
- тексты и графика листинга Play готовы (`docs/store/play/`), сырые кадры скриншотов есть
- Xcode 26.5 — требованиям к минимальному SDK заведомо удовлетворяет

Чего нет: `PrivacyInfo.xcprivacy`, `ITSAppUsesNonExemptEncryption`, страницы поддержки,
метаданных App Store (subtitle, keywords), скриншотов в размерах Apple, записи приложения
в App Store Connect.

---

## Ход выполнения (обновляется по мере работы)

| Задача | Состояние |
|---|---|
| 1. ASC: соглашения, статус трейдера, запись приложения | **за владельцем**, 12.09.2026 — в работе |
| 2. Правки проекта под distribution | **сделано и проверено сборкой** 12.09.2026 |
| 3. Privacy manifest | **сделано и проверено сборкой** 12.09.2026 — файл лежит в корне `.app` |
| 4. Страница поддержки | написана 12.09.2026; **осталось опубликовать** поддеревом |
| 5. Тексты листинга | **сделано** 12.09.2026 |
| 6. Скриншоты 1320×2868 | **сделано** 12.09.2026 |
| 7–9. Архив, TestFlight, листинг | за владельцем |
| 10. Наблюдение и решение по MapLibre | после релиза |

---

## Решения владельца — агент их не пересматривает

| Решение | Значение | Почему так |
|---|---|---|
| Обфускация релиза | **не делается** | на iOS нет аналога R8: Kotlin/Native — машинный код, Apple обфускации не требует. Единственное, что нужно от релизной сборки, — сохранённые `.dSYM` (уже автоматизировано в `iosApp/Scripts/archive-symbols.sh`) |
| iPad | **сейчас `TARGETED_DEVICE_FAMILY = "1"`** | расширение `"1"` → `"1,2"` после релиза — обычное обновление без потери отзывов и установок; обратное направление лишает уже установивших iPad-пользователей обновлений. На iPad приложение работает в режиме совместимости |
| Зависание MapLibre в фоне | **не чинится до релиза** | класс инцидента ограничен эффективно-двухъядерными iPhone (A9/A10 = ровно те, что застряли на iOS 15). Сначала собирается статистика установок по моделям, потом принимается решение. План фикса готов и лежит: `.claude/plans/ios-maplibre-pause-observer.md` |
| `IPHONEOS_DEPLOYMENT_TARGET` | **15.0, не поднимать** | подъём до 16.0 отсёк бы ровно группу риска, но вместе с ней — единственное устройство, на котором баг воспроизводится (iPhone SE 2016 владельца) |
| Bundle ID | `leshy.mushrooms.map` | привязывается к записи App Store навсегда |
| `MARKETING_VERSION` | совпадает с `leshy.versionName` Android | одна версия приложения в обоих магазинах |
| Политика конфиденциальности | тот же URL, что в Play | требование обоих магазинов — открываться без авторизации |
| Тексты и картинки листинга | переиспользуются из `docs/store/play/` | дописываются только поля, которых в Play нет |
| Локали листинга | **en-US и ru-RU**, primary language записи — **English (U.S.)** | как в Play. Primary — фолбэк для всех стран без своей локализации, меняется потом тяжело; остальные языки добавляются позже |
| Доступность | все регионы | ЕС открывается ответом про статус трейдера (задача 1); материковый Китай требует номер ICP-регистрации — проверить на шаге доступности и при отсутствии снять галочку |

**Что агенту запрещено:** поднимать deployment target, включать iPad, начинать фикс MapLibre,
править `project.pbxproj` без последующей проверки по разделу «Имя продукта живёт только в
`Config.xcconfig`» (`iosApp/CLAUDE.md`) — у этого файла в проекте уже была история с
обнулением `PRODUCT_NAME`.

---

## Задача 1. App Store Connect: соглашения, статус трейдера, запись приложения

**Исполнитель: владелец.** Запускается первой — часть шагов ждёт ответа Apple.

1. **Agreements, Tax and Banking** — принять соглашение для бесплатных приложений. Без него
   приложение не может быть распространено, и это выясняется в самом конце.
2. **Статус трейдера для ЕС.** Требование европейского Digital Services Act: маркетплейс
   обязан показывать контактные данные продавца-профессионала. Apple сделала из этого
   обязательную декларацию — **ответить нужно в любом случае**, иначе приложение просто не
   показывается в витринах ЕС, а для приложения с подборками по пятнадцати европейским странам
   это перечёркивает половину смысла.

   Где: App Store Connect → **Business** (раздел с соглашениями и налоговыми данными) → пункт
   про **trader status**; точное название пункта Apple периодически меняет, ориентир — слово
   «trader». Отвечает владелец учётной записи.

   Что отвечать. «Трейдер» в терминах ЕС — тот, кто действует в целях своей торговой,
   предпринимательской или профессиональной деятельности. Бесплатное приложение физлица без
   рекламы, без встроенных покупок и без донатов под это, как правило, не подпадает.

   | Ответ | Что за этим следует |
   |---|---|
   | **не трейдер** | адрес и телефон публично на странице приложения не показываются, верификация не нужна |
   | трейдер | Apple проверяет адрес, телефон и email, после чего публикует их в карточке приложения; проверка занимает время |

   **Формулировку в консоли прочитать самостоятельно и ответить по факту** — это юридическое
   заявление от своего имени, агент за вас его не интерпретирует. Если в приложении когда-нибудь
   появится любая монетизация, включая донаты, статус придётся сменить на «трейдер».
3. Зарегистрировать **Bundle ID** `leshy.mushrooms.map` (или дать Xcode создать его при первом
   архиве) и завести **запись приложения**:
   - **Name** — «Mushroom Map from Leshy» (23 символа, из `docs/store/appstore/listings.json`);
   - **Primary language — English (U.S.)**: это фолбэк для всех стран, где своей локализации нет,
     и меняется он потом тяжело;
   - категория и SKU — на усмотрение владельца (SKU виден только в консоли).
4. Заполнить **App Review Contact** — имя, телефон, email для связи ревьюера (не публичное
   поле, страницей поддержки не заменяется).

**Критерий готовности:** запись приложения существует, в ней нет предупреждений про соглашения
и статус трейдера, билды к ней можно загружать.

---

## Задача 2. Правки проекта под distribution

**Исполнитель: агент. Модель: Sonnet.** Механическая правка конфигов.

**Сделано 12.09.2026** — правки внесены, проверки пройдены, контрольная сборка под симулятор
показывает в бандле 42 локализации и `ITSAppUsesNonExemptEncryption = false`. Ручных действий
в Xcode не потребовалось (см. про синхронизированные папки в пункте 4).

1. `iosApp/iosApp/Info.plist` — добавить:

```xml
<key>ITSAppUsesNonExemptEncryption</key>
<false/>
```

   Приложение ходит только по HTTPS к OpenFreeMap, это штатное исключение экспортного
   контроля. Без ключа билд в TestFlight повисает со статусом Missing Compliance и не
   раздаётся тестировщикам.

2. `iosApp/iosApp.xcodeproj/project.pbxproj`:
   - в **обеих** конфигурациях таргета `TARGETED_DEVICE_FAMILY = "1,2"` → `"1"`;
   - в конфигурации **Release** `CODE_SIGN_IDENTITY = "Apple Development"` →
     `"Apple Distribution"`. В Debug оставить как есть — там она и нужна.

3. Проверить `TEAM_ID=` в `iosApp/Configuration/Config.xcconfig`: переменная пустая. Если
   `grep -rn "TEAM_ID" iosApp/` не находит ни одного её использования — **не трогать**,
   подпись берёт `DEVELOPMENT_TEAM` из `project.pbxproj`.

4. **Дозаполнить локализации бандла.** В приложении 42 языка интерфейса (`AppLanguage.kt`),
   а в `iosApp/iosApp/` только 33 папки `.lproj`; нет девяти — `bs`, `da`, `el`, `is`, `mk`,
   `nb`, `nl`, `pt`, `sq` (партия `europe-15`). Завести каждой свой
   `<код>.lproj/InfoPlist.strings` с `CFBundleDisplayName` — **коротким ярлыком** в стиле
   существующих (`"Грибная карта"`, `"Pilzkarte"`), а не полным `StringKey.AppName`
   («Mushroom Map from Leshy»); названия на этих языках уже есть в
   `shared/…/i18n/strings/Strings<Xx>.kt`.

   **В таргет их добавлять не надо.** Проект собран на синхронизированных папках Xcode 16
   (`PBXFileSystemSynchronizedRootGroup`, `objectVersion = 77`): каталог `iosApp/iosApp`
   принадлежит таргету целиком, и любой новый файл в нём попадает в сборку сам. Список
   `knownRegions` в проекте уже содержит все 42 языка.

   Зачем это в релизной задаче: список языков на странице приложения в App Store выводится
   из локализаций **бандла**, а не из локализаций листинга, — без этих девяти файлов языки не
   попадут в карточку, а ярлык на домашнем экране останется базовым «Mushroom Map».

5. Записать в `iosApp/CLAUDE.md` короткий раздел про номер билда: `CURRENT_PROJECT_VERSION`
   — аналог `versionCode`, обязан расти на каждую загрузку в пределах одного
   `MARKETING_VERSION`, иначе App Store Connect отвечает «The bundle version must be higher
   than the previously uploaded version».

**Критерий приёмки:**

```bash
./gradlew :shared:compileKotlinIosArm64
grep -n "productReference\|path = .*\.app" iosApp/iosApp.xcodeproj/project.pbxproj
```

Первое проходит; во втором **нигде нет** `path = .app` (обнулившееся `PRODUCT_NAME` — известная
ловушка проекта, см. `iosApp/CLAUDE.md`). `git diff` содержит ровно три смысловые правки и
ничего больше.

---

## Задача 3. Privacy manifest

**Исполнитель: агент пишет файл, владелец добавляет его в таргет.**
**Модель: Sonnet.**

**Сделано 12.09.2026** (`iosApp/iosApp/PrivacyInfo.xcprivacy`, `plutil -lint` проходит).
Контрольная сборка подтверждает: файл лежит в корне `leshy.app`. Добавлять его в таргет руками
не понадобилось.

Privacy manifest — машиночитаемый файл внутри бандла, не имеющий отношения к анкете App
Privacy в консоли. Без него после загрузки приходит письмо `ITMS-91053: Missing API
declaration`, блокирующее публикацию.

1. Агент создаёт `iosApp/iosApp/PrivacyInfo.xcprivacy`:
   - `NSPrivacyTracking` = `false`, `NSPrivacyTrackingDomains` — пустой массив;
   - `NSPrivacyCollectedDataTypes` — пустой массив (приложение не собирает ничего: нет
     аналитики и крашлитики, единственный сетевой адрес — тайлы OpenFreeMap, всё хранится
     на устройстве; фактура — `site/README.md`, раздел «Что в тексте привязано к коду»);
   - `NSPrivacyAccessedAPITypes` — категории, которые даёт не свой код, а библиотеки,
     влинкованные в `Shared.framework`:
     - `NSPrivacyAccessedAPICategoryFileTimestamp`, причина `C617.1` (файлы внутри
       контейнера приложения) — okio/DataStore/Room;
     - `NSPrivacyAccessedAPICategoryDiskSpace`, причина `E174.1` — проверка свободного
       места перед записью (офлайн-паки карты).

   У `MapLibre.framework` собственный манифест есть, дублировать его не нужно.

2. Добавлять файл в таргет не нужно — каталог `iosApp/iosApp` синхронизирован с таргетом
   целиком (см. задачу 2, пункт 4). Правка `project.pbxproj` здесь не требуется вовсе, и это
   хорошо: цена ошибки в этом файле для проекта известна.

3. Проверка после сборки — файл обязан лежать в корне бандла:

```bash
find iosApp/build -name PrivacyInfo.xcprivacy -path "*leshy.app/*" -not -path "*Frameworks*"
```

**Про итерации:** список задекларированных категорий — гипотеза по составу зависимостей.
Если после загрузки придёт ITMS-91053, письмо **называет конкретный API и категорию** —
дописать её в манифест и загрузить следующий билд. Это штатный цикл, а не ошибка планирования.

**Критерий приёмки:** файл валиден (`plutil -lint`), попадает в корень `.app`, в нём нет
задекларированных типов собираемых данных.

---

## Задача 4. Страница поддержки

**Исполнитель: агент пишет страницу, владелец публикует.** **Модель: Sonnet.**

**Страница написана 12.09.2026** (`site/support.html`, `site/README.md` обновлён).
Осталось владельцу: `git subtree push --prefix site site main`.

App Store Connect требует **Support URL** — обязательное поле; у Google Play аналога нет,
поэтому в комплекте его не существует. Страница должна открываться без авторизации и
относиться именно к этому приложению.

1. Агент пишет `site/support.html` по образцу `site/privacy.html`: самодостаточный файл без
   внешних CSS, шрифтов и скриптов, те же четыре языка (ru, en, fr, es) с тем же
   переключателем, **полноценный без JavaScript**, адрес `fefradkin@gmail.com` вписан руками
   в каждую языковую версию. Содержание: что это за приложение, куда писать об ошибке и с
   какой информацией (модель устройства и версия iOS — это понадобится для наблюдения из
   задачи 10), ссылка на политику конфиденциальности.
2. Агент дописывает `site/README.md`: вторая страница, тот же способ публикации, и адрес
   `https://leshy-mapper.github.io/mushrooms-map/support.html` идёт в App Store Connect.
3. Владелец публикует: `git subtree push --prefix site site main`.

**Критерий приёмки:** страница открывается по своему URL, с выключенным JavaScript показывает
все четыре языковые версии подряд и не теряет ни одного адреса; файл в репозитории совпадает
с опубликованным.

---

## Задача 5. Тексты листинга App Store

**Исполнитель: агент. Модель: Sonnet.**

**Сделано 12.09.2026** — `docs/store/appstore/` заведён, все поля в лимитах, заметки ревьюеру
написаны.

1. Завести `docs/store/appstore/` по образцу `docs/store/play/`: `listings.json` источником
   правды, `README.md` с таблицей файлов.

   **Как это попадает в консоль.** У App Store Connect, в отличие от Play, импорта метаданных
   из файла в вебе нет: поля заполняются руками по локали (переключатель языка в записи
   приложения) либо через App Store Connect API / `fastlane deliver`. Ради двух локалей API не
   заводим — значит из `listings.json` генерируется по файлу на локаль
   (`listing_en-US.md`, `listing_ru-RU.md`) для копипасты, ровно как сделано в
   `docs/store/play/`. CSV здесь не нужен вовсе.
2. Поля и лимиты App Store:

   | Поле | Лимит | Откуда берётся |
   |---|---|---|
   | `title` (имя в сторе) | 30 | из Play без изменений (сейчас 23 символа) |
   | `subtitle` | **30** | **новое.** Короткое описание Play — 70–72 символа, не влезает, ужимать |
   | `keywords` | **100**, через запятую | **новое, аналога в Play нет вообще** |
   | `description` | 4000 | из Play без изменений (3156 en / 2732 ru) |
   | `promotional_text` | 170, необязательное | можно не заводить |

3. Проверку длин сделать по образцу `convert_listings.py` (он отказывается писать файл, если
   поле длиннее лимита, и проверяет **каждую локаль отдельно**).
4. Написать **App Review Notes** (на английском, отдельным файлом
   `docs/store/appstore/review-notes.md`): зачем приложению фоновая геолокация (запись трека
   при выключенном экране и свёрнутом приложении, `UIBackgroundModes: location`,
   `allowsBackgroundLocationUpdates` включается только на активной записи); как посмотреть
   функциональность без выхода в лес — импорт демо-архива через Настройки → Экспорт/Импорт
   (`docs/store/play/demo-data/leshy-demo-en.zip`); что приложение не определяет съедобность
   и дисклеймер показан в онбординге и на карточках видов.
5. Ограничения на текст те же, что соблюдены для Play: без превосходных степеней, CAPS,
   эмодзи в названии, упоминаний цен и других магазинов.

**Критерий приёмки:** все поля обеих локалей проходят проверку лимитов; в `en-US.subtitle` и
`ru-RU.subtitle` ≤30 символов; keywords заполнены и не повторяют слова из названия.

---

## Задача 6. Скриншоты в размерах App Store

**Исполнитель: агент. Модель: Sonnet.**

**Сделано 12.09.2026** — по восемь кадров 1320×2868 на локаль в
`docs/store/appstore/graphics/phone-{en,ru}/`.

Переснимать **не нужно**: в кадрах срезаны статус-бар и жестовая полоса (`CROP_TOP,
CROP_BOTTOM = 70, 2330` в `compose_shots.py`), рамки устройства нет, интерфейс — Compose
Multiplatform, на iOS он тот же. Нужен другой размер холста.

1. Параметризовать `docs/store/play/compose_shots.py` по размеру холста (сейчас `W, H =
   1080, 1920` и ширина вписывания `900` зашиты константами) — добавить пресеты
   `play` (1080×1920) и `appstore` (1320×2868, комплект 6.9" iPhone), кегль заголовка и
   ширину вписывания масштабировать пропорционально. Скрипт остаётся общим для двух
   магазинов, сырые кадры не трогаются.
2. Прогнать на существующих сырых кадрах, результат — в `docs/store/appstore/graphics/phone-en/`
   и `phone-ru/`.
3. Качество проверить глазами: исходный кадр 1080 px вписывается примерно в 1100 px, то есть
   увеличение около 2% — деградации быть не должно.

Комплект для iPad **не готовить** — `TARGETED_DEVICE_FAMILY = "1"` его не требует.

**Критерий приёмки:** восемь файлов на локаль, каждый ровно 1320×2868; комплект Play не
изменился (проверить `git status` по `docs/store/play/graphics/`).

---

## Задача 7. Первый архив и загрузка билда

**Исполнитель: владелец.** Агент к Xcode и учётной записи не подключается.

1. Перед архивом: схема в конфигурации **Release**, назначение — **Any iOS Device (arm64)**,
   `CURRENT_PROJECT_VERSION` в `Config.xcconfig` увеличен, если это не первая загрузка.
2. Product → Archive → Distribute App → App Store Connect. Xcode при первом архиве сам
   выпустит distribution-сертификат и профиль (подписка оформлена, `DEVELOPMENT_TEAM`
   прописан). Если Organizer ругается на сертификат — смотреть `CODE_SIGN_IDENTITY` из
   задачи 2.
3. Дождаться обработки билда. Разобрать письма от App Store Connect: `ITMS-91053` —
   дописать манифест из задачи 3; Missing Compliance — проверить, что ключ из задачи 2
   попал в бандл.
4. Сохранить архив: `.dSYM` понадобятся для символизации; локальный архив сборок уже ведёт
   `iosApp/Scripts/archive-symbols.sh`.

**Критерий готовности:** билд в App Store Connect в состоянии, пригодном для TestFlight, без
предупреждений об экспортном соответствии и без писем ITMS.

---

## Задача 8. TestFlight

**Исполнитель: владелец.**

TestFlight не требует ни скриншотов, ни описания, ни ключевых слов — раздать сборку можно
до готовности листинга (задачи 5 и 6 идут параллельно).

1. **Internal** (до 100 человек, добавленных пользователями App Store Connect) — без ревью,
   билд доступен сразу после обработки.
2. **External** (до 10 000, приглашение письмом или публичной ссылкой) — через облегчённый
   Beta App Review.
3. В «What to Test» попросить прицельно:
   - **свернуть и развернуть приложение во время активной записи прогулки**, несколько раз
     подряд, с открытым экраном карты — это ровно тот сценарий, который трижды убил
     приложение на iPhone SE 2016;
   - в любом сообщении об ошибке указывать **модель устройства и версию iOS** — без модели
     репорт не отличить от класса инцидента, ограниченного старым железом;
   - долгая прогулка с заблокированным экраном (проверка фоновой записи трека).
4. Цель набора тестировщиков — **разные устройства, преимущественно современные**: базовая
   линия нужна именно по ним.

**Критерий готовности:** external-группа получила билд; собраны репорты минимум с нескольких
разных моделей.

---

## Задача 9. Листинг и отправка в ревью

**Исполнитель: владелец**, тексты и файлы готовит агент в задачах 4–6.

Заполнить в записи приложения:

- имя, subtitle, описание, keywords (задача 5), локализации en-US и ru-RU;
- скриншоты 6.9" iPhone (задача 6) — Apple масштабирует остальные размеры сама;
- **Support URL** (задача 4) и **Privacy Policy URL** (тот же, что в Play);
- **App Privacy** — своя анкета, из Data Safety не импортируется: «Data Not Collected»
  (ни аналитики, ни крашлитики, данные не покидают устройство; тайлы карты запрашиваются у
  OpenFreeMap, но разработчику никакие данные не передаются);
- **возрастной рейтинг** — анкета; дисклеймер про несъедобность в приложении уже есть
  (`MushroomImageDisclaimerBanner`, `OnboardingScreen`) и вынесен отдельным блоком в описание;
- категорию, ценообразование (бесплатно) и доступность по странам;
- **App Review Notes** и демо-архив из задачи 5;
- экспортное соответствие — закрыто ключом из задачи 2.

**Критерий готовности:** отправлено в ревью; на руках есть текст заметок ревьюеру и понимание,
что делать при отказе по 2.1 (фоновое зависание — см. задачу 10).

---

## Задача 10. После релиза: наблюдение и решение по MapLibre

**Исполнитель: владелец смотрит консоль, агент выполняет план фикса, когда решение принято.**

1. **App Store Connect → Analytics** даёт разбивку установок и сессий **по моделям
   устройств**, крэши и зависания — тоже по моделям. Это прямой ответ на вопрос «сколько
   пользователей в зоне риска»: интересуют iPhone 6s / 6s Plus / SE (2016) / 7 / 7 Plus —
   эффективно двухъядерные A9/A10, они же ровно те, кто застрял на iOS 15.
2. Признак того самого класса инцидентов: `EXC_CRASH (SIGKILL)`, `0x8BADF00D`, FRONTBOARD,
   стек главного потока с кадрами `MapLibre.framework` и `std::future<void>::get()`.
3. Когда решение принято — выполнять `.claude/plans/ios-maplibre-pause-observer.md` (снятие
   наблюдателя `pauseFileSource:`, около десяти строк в `PlatformModule.ios.kt`). Проверять
   **только на устройстве**: карта, активная запись, свернуть-развернуть 20 раз подряд. У
   правки есть своя цена — MapLibre продолжит работать с SQLite в фоне, следить за
   `disk I/O error` в логах долгих прогулок с заблокированным экраном.
4. Независимая от ядер ветка риска на тех же старых устройствах — **память**: измеренный пик
   591 МБ `lifetimeMax` при 2 ГБ на борту. В расследовании эта ветка не заведена; если в
   аналитике окажутся заметные потери сессий на старых моделях, начинать надо с неё, а не с
   фикса из пункта 3.

---

## Чего в этом плане намеренно нет

- **Обфускации и минификации** — на iOS их нет как класса, см. таблицу решений.
- **Комплекта скриншотов iPad** и поддержки iPad — отдельная задача после релиза.
- **Фикса зависания MapLibre** — задача 10.
- **Починки линковки iOS-тестов** (`:shared:linkDebugTestIosSimulatorArm64` падает на
  конфликте `kotlinx.datetime.Clock` / `kotlin.time.Clock`, см. корневой `CLAUDE.md`) — на
  публикацию не влияет, в релизной сборке не участвует.
