package compose.project.leshy.domain.model

/**
 * Bounds and default for the user-configurable multiplier applied to the base mushroom marker icon
 * size shown on maps (`SettingsScreen`'s "mushroom size" slider) — as low as half, as high as
 * double, the size the app ships with by default.
 */
const val MUSHROOM_MARKER_SIZE_SCALE_MIN = 0.5f
const val MUSHROOM_MARKER_SIZE_SCALE_MAX = 2f
const val MUSHROOM_MARKER_SIZE_SCALE_DEFAULT = 1f
