# План: подборки грибов по странам

Статус: **Phase 2 готов** (Phase 3 следующий). Отмечать фазы `[x]` по мере
выполнения. Каждая фаза — отдельная сессия. Не переносить сюда код/детали
реализации после того, как фаза сделана — итоговые неочевидные решения идут
в `presentation/CLAUDE.md` / `data/CLAUDE.md`, этот файл только про план
вперёд. Когда все фазы, включая Phase 5, закрыты — файл можно удалить.

## Идея

Сейчас каталог из 30 видов — единая («базовая») подборка. Нужно разбить его
на подборки по странам, дать пользователю выбирать, какие подборки/виды ему
интересны, и не потерять при этом уже собранные данные при переключении
подборок. Демо-подборки (произвольно нарезанные из текущих 30) — на первых
фазах, финальные подборки по странам — отдельно, позже (Phase 5).

## Модель данных (два независимых флага + одна денормализация)

| Поле | Смысл | Пишет | Читает |
|---|---|---|---|
| `Category.isActive` (уже есть, не менять напрямую) | «показывать на Карте и в ленте Записи» | экран Фильтра (как сейчас) **+ каскадный сброс из пересчёта ниже** | Map, Record — без изменений |
| `Category.isPicked` (новое) | «пользователь выбрал этот гриб через подборки» | пикер подборок (Настройки + первый запуск) | используется при пересчёте `isFilterEligible`/каскада `isActive` |
| `Category.isFilterEligible` (новое, денормализация) | «показывать в списке экрана Фильтра» | пересчитывается целиком при каждом сохранении пикера подборок (+ разово при старте приложения) | экран Фильтра — простой `WHERE`, без JOIN/EXISTS |

Правило пересчёта (по всем категориям разом, при сохранении пикера или
старте приложения):

```
isFilterEligible = isPicked || existsFieldMark(categoryId)
if (!isFilterEligible) isActive = false   // каскад, см. ниже
```

**Каскад `isActive` — решено 2026-08-18, во время разбора Phase 1 вживую.**
Без него открывается дыра: у всех 30 базовых видов `isActive=true` с
момента ДО появления подборок, так что снятие «пикнутости» в Настройках
само по себе ничего не меняло на Записи/Карте — пользователь должен был бы
ещё отдельно зайти в Фильтр и снять `isActive` там. А если находок по виду
нет, он к этому моменту уже пропал бы из списка Фильтра (не эту фазу, а
из-за `isFilterEligible`) — то есть выключить `isActive` стало бы вообще
неоткуда. Фикс: как только пересчёт делает `isFilterEligible=false` для
категории — тем же проходом гасить `isActive=false`. Безопасно применять
безусловно на каждый пересчёт: пока категория `isFilterEligible=false`, её
нет в списке Фильтра, значит `isActive` в принципе не мог смениться на
`true` каким-то другим путём между пересчётами — перезаписывать нечего.
Если находки появились ПОЗЖЕ (через уже видимые на Записи виды это
невозможно — по построению они и так `isActive`; спорный сценарий не
возникает).

Сортировка на экране Фильтра: `isPicked DESC`, затем обычный порядок
категорий — «унаследованные» виды (уже есть находки, но подборка снята)
уходят вниз списка, но не пропадают, пока не собраны существующим правилом.

**Известный краевой случай (сознательно не решается сейчас):** если у
непикнутого «унаследованного» вида удалить последнюю находку (кнопка «−»),
`isFilterEligible` не сбросится немедленно — вид останется в нижней группе
Фильтра до следующего сохранения пикера. Безвредно (пустой тумблер), само
исправляется при следующем сохранении подборок — не стоит городить пересчёт
на каждое удаление находки ради этого.

`collections`/`category_collections` — новые таблицы, `category_collections`
— много-ко-многим (один гриб может входить в несколько подборок стран).
Подборки не хранят собственный флаг «выбрана» — это производное состояние
(все/часть/ни один участник `isPicked=true`), чекбокс всей подборки в UI —
просто bulk-запись `isPicked` по всем её участникам.

## Mac vs Windows

Вся фича — `shared/commonMain` (Room + DataStore + Compose), без
`expect`/`actual`, без нативных SDK. Ждать Mac не нужно — компиляция iOS
проверяется как обычно (`:shared:compileKotlinIosArm64` /
`compileKotlinIosSimulatorArm64`) на Windows по ходу дела. Один Mac-сеанс
приберечь на Phase 4 — визуальная проверка на симуляторе разом для всех
новых/изменённых экранов, вместо переключения машины на каждой фазе.

## Фазы

- [x] **Phase 0 — Схема.** Room v4→v5: таблицы `collections`,
  `category_collections` (many-to-many, `ON DELETE CASCADE` в обе стороны),
  новые колонки `categories.isPicked`/`isFilterEligible` (`DEFAULT 1`).
  `EnsureDefaultCollectionsUseCase` сидирует 3 демо-подборки (north/south/
  east), нарезанные из нынешних 30 видов, с намеренным пересечением двух пар
  видов между соседними подборками (проверка many-to-many). Тот же
  upsert-diff паттерн, что `EnsureDefaultCategoriesUseCase`; вызывается сразу
  после него из `RecordViewModel.init` (тот же call site, что и раньше — не
  новый). Домен: `Collection`, `CollectionRepository`/`Impl`, `Category`
  получил `isPicked`/`isFilterEligible` (default `true`, дописаны в конец
  конструктора — позиционные вызовы вроде `RecordScreen.PREVIEW_CATEGORIES`
  не потребовали правок). DI (`DataModule`/`DomainModule`/
  `PresentationModule`) обновлён. Без UI — Filter/Settings/Record не
  тронуты, эта фаза только про схему и сидирование.
  Проверено: `:shared:compileAndroidMain` и `:shared:
  compileCommonMainKotlinMetadata` зелёные, экспортированная `5.json`
  структурно совпадает с миграцией. `:shared:compileKotlinIosSimulatorArm64`
  на этой Windows-машине сам по себе `SKIPPED` (cinterop MapLibre недоступен
  без Xcode — существующее ограничение, не новое), но фаза не добавила
  никакого платформенного (`iosMain`) кода, так что риска здесь нет; полная
  iOS-проверка — как и планировалось, в Phase 4 на Mac.
  `data/CLAUDE.md` обновлён (версия схемы, новые таблицы/колонки, v3→v4/
  v4→v5 миграции, которые раньше не были описаны).

- [x] **Phase 1 — Компонент пикера подборок.** `ui/components/
  CollectionPicker.kt` — раскрывающийся список подборок, `TriStateCheckbox`
  на подборку целиком (состояние — производное от `CollectionPickerItem.
  pickState`, `presentation/CollectionPicker.kt`), обычный `Checkbox` +
  `MushroomPhoto` на каждый гриб внутри. Подключено в Настройки
  (`SettingsViewModel`/`SettingsScreen`) под новым заголовком «Подборки
  грибов»; `SetCollectionPickedUseCase` — bulk-запись `isPicked` по всем
  участникам подборки. Экран первого запуска (Phase 3) переиспользует тот же
  композабл, не пересобирает.

  **Две реальные регрессии найдены и исправлены живым прогоном на
  эмуляторе (не только компиляцией) — подробности в `data/CLAUDE.md`:**
  забытая DI-регистрация `SetCollectionPickedUseCase` (краш при открытии
  Настроек); и главное — `CategoryRepositoryImpl`/`CollectionRepositoryImpl`
  `upsert` через `INSERT OR REPLACE` молча стирал(и) `category_collections`
  через `ON DELETE CASCADE` из Phase 0 при любом обычном тумблере
  `isActive`/`isPicked` (REPLACE = DELETE+INSERT на уровне SQLite) — фикс на
  настоящий `@Update` по `id`. Заодно всплыла третья, смежная: diff в
  `EnsureDefaultCategoriesUseCase` оберегал от отката только `isActive`,
  новые `isPicked`/`isFilterEligible` откатывались на дефолт при каждом
  повторном вызове (Запись/Настройки) — добавлены в список сохраняемых
  полей.

  Проверено вживую на эмуляторе (не только компиляцией): APK
  переустановлен ПОВЕРХ существующей БД (реальная миграция v4→v5, не с
  нуля), пикер открыт, подборка развёрнута, отдельный вид снят/возвращён
  (проверено через прямой дамп `category_collections`/`categories` —
  membership и `isPicked` не теряются), тумблер всей подборки — все три
  визуальных состояния (✓/пусто/минус) подтверждены скриншотами.

- [x] **Phase 2 — Пересчёт `isFilterEligible` (+ каскад `isActive`) + запрос
  экрана Фильтра.** `RecalculateFilterEligibilityUseCase` (правило + каскад
  выше) — вызывается из `SettingsViewModel`/`RecordViewModel.init` (рядом с
  `ensureDefaultCategories`/`ensureDefaultCollections`, тот же idempotent
  multi-call-site паттерн) и из `SettingsViewModel.toggleCollection`/
  `setCategoryPicked` сразу после записи `isPicked`. Реализован через
  `observeAll().first()` на `CategoryRepository`/`FieldMarkRepository` —
  без новых DAO-запросов, тот же паттерн, что `BackfillWalkThumbnailsUseCase`.
  `CategoryDao`/`CategoryRepository` получили `observeFilterEligible()`
  (`WHERE isFilterEligible = 1`) — `MapFilterViewModel` читает его вместо
  `observeAll()`, сортировка `isPicked DESC` через `partition` поверх
  обычного `sortCategories()` (порядок внутри каждой группы не ломается,
  `partition` сохраняет относительный порядок).

  Проверено вживую на эмуляторе (не только компиляцией), APK переустановлен
  поверх существующей БД: непикнутый вид без находок (`Вешенка`) —
  `isActive` каскадом ушёл в `0` при заходе на Настройки, подтверждено
  прямым дампом `categories`; непикнутый вид С находками (`Подберёзовик`,
  временно снят для теста) — `isFilterEligible` осталось `1`, `isActive` не
  тронут (каскад не сработал, как и задумано), в диалоге Фильтра вид не
  пропал, а уехал в самый низ списка; `Вешенка` (действительно
  `isFilterEligible=0`) корректно пропала из диалога Фильтра целиком;
  `category_collections` membership пережил оба пересчёта (проверка на
  регресс REPLACE-бага из Phase 1). Тестовое состояние `Подберёзовик`
  возвращено обратно в пикнутое после проверки.

- [ ] **Phase 3 — Экран первого запуска.** Новый одноразовый DataStore-флаг +
  маршрут, оборачивающий компонент из Phase 1, перед Home. В основном
  навигационная обвязка — данные и UI уже готовы с Phase 1/2.

- [ ] **Phase 4 — iOS/визуальный проход.** Тот самый Mac-сеанс: пикер в
  Настройках, экран Фильтра (новая группировка), экран первого запуска — на
  симуляторе. Заодно — короткая заметка в `presentation/CLAUDE.md` (раздел
  MapFilter) про модель из двух флагов + денормализацию, аналогично тому,
  как там уже описано про `isActive`/Record. После этого шага план в этом
  файле для фаз 0–4 можно считать закрытым.

- [ ] **Phase 5 — позже, отдельная инициатива.** Заменить демо-подборки на
  финальные по странам — правка сид-данных в
  `EnsureDefaultCollectionsUseCase`, та же upsert-diff миграция, схему
  трогать не нужно. Тогда же решить: удалять базовый каталог из 30 видов или
  оставить его как одну из подборок среди прочих.
