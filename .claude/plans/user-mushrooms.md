# План: пользовательские грибы (свой вид + своя иллюстрация)

Статус: **Phase 4 готов** (Phase 3, редактор-ластик, пропущена — форма
создания работает без него, см. ниже; следующая — Phase 5, довести
интеграцию по остальным экранам). Отмечать фазы `[x]` по мере выполнения.
Каждая фаза — отдельная сессия. Не переносить сюда код/детали реализации после
того, как фаза сделана — итоговые неочевидные решения идут в `data/CLAUDE.md` /
`presentation/CLAUDE.md` / `ui/map/CLAUDE.md` / `iosMain/CLAUDE.md`, этот файл
только про план вперёд. Когда все фазы закрыты — файл можно удалить.

## Идея

Раздел «Мои грибы»: пользователь заводит свой вид — название на текущем языке
приложения, научное название, съедобность, цвет, — и прикрепляет своё фото,
которое тут же редактирует встроенным ластиком: замазывает всё, что не гриб,
до прозрачности. Результат сжимается до формата остальных иллюстраций
приложения (**400 px по длинной стороне**, ср. `composeResources/drawable/*.webp`
— 400×267, 20–30 КБ) и дальше работает ровно как любой из 30 каталожных видов:
плитка на Записи, маркер на Карте, участие в Фильтре/подборках/статистике.

Ключевое ограничение плана: **новых зависимостей — ноль** (см. «Решение по
зависимостям»), рост бинарника — десятки килобайт кода, штатные экраны не
замедляются.

## Модель данных (Room v5→v6, аддитивная)

Пользовательский вид — не отдельная таблица, а строка в существующих
`categories` с флагом. Так весь код Записи/Карты/Фильтра/подборок/статистики,
который уже работает с `Category`, начинает работать с пользовательскими видами
без единой правки запросов.

| Поле | Смысл |
|---|---|
| `source TEXT NOT NULL DEFAULT 'APP'` | происхождение вида: `APP` (каталожный) / `USER` (создан здесь) / `IMPORTED` (приехал из чужого архива). Enum `CategorySource`, хранится текстом через `Converters` — ровно как `EdibilityStatus`, новых механизмов не нужно. `EnsureDefaultCategoriesUseCase` про не-`APP` строки ничего не знает и не трогает их (он итерирует по `DEFAULT_CATEGORIES`, а не по таблице) |
| `customNames TEXT` | JSON `{"ru":"...","en":"..."}`. Языков всего два (`AppLanguage`), по имени никогда не делается запрос — отдельная таблица переводов была бы overkill; `Converters`/kotlinx.serialization уже в проекте |
| `scientificName TEXT` | латинское название, одинаковое на всех языках |
| `iconFile TEXT` | **имя файла, не абсолютный путь** — см. ниже |

**`source` — три состояния вместо булева флага, и это не усложнение.** Почти
везде код спрашивает ровно одно: `source == APP` или нет (не-`APP` → показывать в
«Моих грибах», выгружать в архив, разрешать редактировать). Отдельный `IMPORTED`
нужен ради одного конкретного правила слияния при импорте (см. ниже) и ради
честной пометки в UI «этот вид приехал из чужого архива» — без него пришлось бы
угадывать, чей это вид, а при повторном экспорте — гадать, выгружать ли его
снова. Ветвлений по трём значениям в коде не появляется, `when` по `source` —
только в двух местах (импорт и подпись в списке).

**`iconFile` хранит имя файла, а не абсолютный путь — сознательно иначе, чем
`objects.photoPath`/`walks.thumbnailPath`.** Те хранят абсолютный путь, и из-за
этого пришлось заводить `RepairPhotoPathsUseCase`: на iOS UUID контейнера
песочницы меняется на части сценариев обновления/восстановления, и все
сохранённые абсолютные пути разом протухают. Повторять эту ошибку в новой
колонке не нужно — путь собирается при чтении через
`PhotoStorage.resolvePath(iconFile)`, и чинить нечего по построению.

**`nameKey` у пользовательского вида — генерируемый технический ключ**
(`user_<millis>_<random>`), а не введённое имя. `nameKey` — ключ синхронизации
в `EnsureDefaultCategoriesUseCase.getByNameKey()` и **единственный** идентификатор
вида в архиве экспорта (`ObjectExportDto.categoryNameKey`); если сделать его
производным от имени, переименование вида после экспорта тихо оторвёт находки от
вида при импорте (упадут в `category_misc`). Имя живёт только в `customNames` и
меняется свободно.

`isPicked`/`isFilterEligible` у своего вида — `true` при создании (пользователь
только что сам его завёл, странно было бы прятать), дальше обычные
пользовательские поля, `RecalculateFilterEligibilityUseCase` работает с ними как
со всеми остальными.

## Только скрытие, полного удаления нет (решено)

Кнопки «удалить вид» в приложении не будет вообще — ни для `USER`, ни для
`IMPORTED`. Причина не только в UX: у `objects.categoryId` внешний ключ на
`categories` **без `onDelete`** (`ObjectEntity.kt`), то есть `NO ACTION`, а Room
включает `PRAGMA foreign_keys` — `DELETE` вида с находками не «снесёт находки
каскадом», а упадёт с FK-constraint. Обходить это (переносом находок в
`category_misc`) значит вводить единственный во всём приложении сценарий
необратимой потери пользовательских данных ради сомнительной пользы.

**Скрытие не требует ни новой колонки, ни нового use case** — это тот же
`SetCategoryPickedUseCase`, которым работает пикер подборок. Но писать **только**
`isPicked=false` недостаточно (первая версия плана так и предполагала — неверно,
поймано вживую в Phase 1): что видно на Записи и на Карте, решает `isActive`, а
каскад в `RecalculateFilterEligibilityUseCase` гасит его лишь тогда, когда вид
перестал быть `isFilterEligible`, то есть **никогда, если у вида есть находки**
(`isFilterEligible = isPicked || есть находки`). Свой вид почти всегда с
находками — иначе зачем его заводили.

Поэтому оба направления пишут `isActive` явно:
- скрыть — `isPicked=false` **и** `isActive=false`;
- вернуть — `isPicked=true` **и** `isActive=true` (каскад работает в одну
  сторону, сам `isActive` обратно не включает — задокументировано в
  `mushroom-collections.md`, Phase 4).

Что при этом остаётся: есть находки → `isFilterEligible` остаётся `1`, вид виден
в списке Фильтра с выключенным тумблером (его находки на карте можно включить
обратно) — это сознательно, иначе включить их было бы уже нечем; находок нет →
пропадает и оттуда, то есть скрыт полностью.

Ещё одно следствие для Phase 4: экран «Мои грибы» перечисляет виды **по
`source`**, а не по `isActive`/`isPicked` — иначе скрытый вид станет недостижим
и его нельзя будет вернуть.

Файл иконки при скрытии не трогается: вид может вернуться, а перезаписать
картинку пользователь и так может через редактирование.

## Резолвер имени и иконки — самая размазанная правка, делается заранее

Сейчас иконка вида достаётся напрямую из мапы ресурсов в **шести** местах:
`MushroomTile.MushroomPhoto`, `MapFilterDialog`, `CollectionPicker`,
`MushroomDonutChart`, `SettingsScreen`, `MushroomMarkerIcon` — везде
`Res.allDrawableResources[iconRef]`. Имя — через `categoryDisplayName(nameKey)`,
который на неизвестном ключе падает обратно на сам ключ (то есть без правок
пользовательский вид покажет `user_1234...` вместо имени).

Поэтому Phase 1 — единый резолвер, **до** любого UI создания видов:
- `CategoryIcon(category, modifier)` — один composable, внутри выбирает
  `painterResource` (каталожный `iconRef`) или Coil по `"file://"` (свой
  `iconFile`). Все шесть мест переводятся на него.
- `categoryDisplayName(category)` / `categoryDisplayName(category, language)` —
  перегрузки поверх нынешних `nameKey`-версий, читают `customNames`. Тянет за
  собой `CategorySorting.kt` (все три функции — сортировка и поиск по имени).

**Маркер на карте — не `AsyncImage`, а паттерн `PlaceMarkerIcon.kt`.** Там уже
решена ровно эта задача (фото из файла как `Painter` для MapLibre): Coil
`rememberAsyncImagePainter` + отдача наружу внутреннего painter'а из
`State.Success` (иначе `onDraw` никогда не срабатывает и Coil декодирует
исходник в полный размер), плюс обязательный `disallowHardwareBitmaps()` — без
него MapLibre роняет приложение на `Software rendering doesn't support hardware
bitmaps`. Не изобретать заново, переиспользовать.

## Картинка: формат, размеры, платформенный слой

- Съёмка/выбор → **рабочая копия ≤1536 px** по длинной стороне сразу при
  загрузке. Редактировать фото с камеры в исходном разрешении (12 Мп ≈ 48 МБ в
  ARGB_8888) — гарантированные фризы и OOM на слабых Android-устройствах.
- Результат → **400 px по длинной стороне, PNG с альфой** (~40–80 КБ). PNG, а не
  WebP: прозрачность нужна обязательно, а WebP-кодирование с альфой на iOS без
  сторонних библиотек не гарантировано; выигрыш в 20 КБ на вид того не стоит.
- Кодирование — единственный настоящий `expect`/`actual` этой фичи:
  `expect fun encodePng(bitmap: ImageBitmap): ByteArray` — Android
  `asAndroidBitmap().compress(PNG)`, iOS — Skiko (`asSkiaBitmap()` →
  `Image.makeFromBitmap(...).encodeToData(PNG)`) или
  `UIImagePNGRepresentation`. Общего решения нет: Skiko есть на iOS (идёт с
  Compose), но на Android Compose рисует не через Skia-обёртку Skiko — правило 6
  CLAUDE.md соблюдено.
- Выбор из галереи (сейчас в приложении только камера) — тоже `expect`/`actual`:
  Android `PickVisualMedia` из уже подключённой `androidx.activity`, iOS
  `PHPickerViewController`. Пишется рядом с `CameraLauncher.android/ios.kt`, тем
  же паттерном.
- Запись байтов на диск — okio + `PhotoStorage`, как в `ImportDataUseCase`.

## Редактор-ластик — чистый Compose, без библиотек

1. Фото рисуется в offscreen-слой
   (`Modifier.graphicsLayer(compositingStrategy = CompositingStrategy.Offscreen)`).
2. Мазки пальцем (`pointerInput` + `Path`) рисуются поверх с
   `BlendMode.Clear` → пиксели уходят в прозрачность.
3. Undo — снять последний мазок; «вернуть» — второй слой мазков поверх исходника
   с обычным `SrcOver`.
4. Прозрачность показывать «шахматкой» под фото, иначе на тёмной теме не видно,
   что стёрлось.
5. Прямоугольный кроп + регулятор размера кисти — дёшево по коду, сильно
   поднимают качество результата.
6. Экспорт — отрисовать итог в `ImageBitmap` через
   `androidx.compose.ui.graphics.Canvas(bitmap)` (commonMain), дальше
   `encodePng`.

**Риск производительности ровно один:** сотни `Path` перерисовываются каждый
кадр. Если на реальном устройстве заметна просадка — «запекать» мазки в растр
каждые N штук (или сразу рисовать в `ImageBitmap`, храня для undo только
последние N мазков).

## Экспорт/импорт своих видов (решено — отдельной папкой в архиве)

Сейчас категории в архив не попадают вообще, а неизвестный `categoryNameKey` при
импорте молча уходит в `category_misc` — то есть без этой части экспорт/импорт
тихо уничтожает свои виды и обезличивает их находки.

**Раскладка архива** — новая папка верхнего уровня рядом с `walk_<id>/`:

```
manifest.json
categories/categories.json     # только строки с source != APP
categories/<nameKey>.png       # иконки тех же строк, если у них есть iconFile
walk_<id>/...                  # как сейчас, не меняется
```

**Что выгружается:** только виды, реально встретившиеся в отобранных прогулках
(экспорт умеет ограничиваться подмножеством прогулок — `walkIds` в
`ExportDataUseCase`), и только с `source != APP`. Каталожные виды не выгружаются
по-прежнему: они пересоздаются на любой установке и резолвятся по `nameKey`.
`IMPORTED` выгружается наравне с `USER` — иначе вид, приехавший с чужого
устройства, терялся бы на следующем же переносе.

**Что происходит на импорте** — по `nameKey`, до разбора прогулок (карта
`categoryIdByNameKey` строится в самом начале `ImportDataUseCase`, значит
категории должны появиться раньше):

| Состояние на устройстве | Действие |
|---|---|
| вида с таким `nameKey` нет | создать строку, `source = IMPORTED`, `isPicked = true`, `isActive = true` (иначе импортированные находки не покажутся на карте и будет выглядеть как потеря данных), скопировать иконку из архива |
| вид есть, иконки нет (`iconFile == null`) | иконку из архива скопировать и привязать, остальные поля **не трогать**; `source` не менять |
| вид есть, иконка есть | не трогать ничего — локальная картинка и локальное имя всегда выигрывают у приезжих |

**Главная асимметрия, которую нельзя проглядеть:** прогулки импорт всегда
вставляет как новые строки и никогда не сливает (это его прямо
задокументированное правило), а категории — **наоборот, сливаются по `nameKey`**.
Иначе повторный импорт того же архива каждый раз плодил бы дубликаты одного вида,
и находки расползались бы по копиям. Ровно поэтому `nameKey` обязан быть
глобально уникальным (`user_<millis>_<random>` из раздела про модель), а не
производным от имени.

Имя файла иконки на устройстве — детерминированное (`catimg_<nameKey>.png`), так
что повторный импорт переписывает тот же файл, а не плодит копии.

**Научное название — не ключ слияния.** С Phase 4 у пользовательского вида
`scientificName` почти никогда не `null` (автозаполнение транслитерацией,
когда поле не заполнено вручную — см. Phase 4 ниже), и два независимо
созданных вида на разных устройствах легко получают одинаковое или похожее
научное название (оба не заполнили поле, оба стартовали от похожего
обиходного имени). Слияние при импорте обязано оставаться строго по
`nameKey` (глобально уникальный `user_<millis>_<random>`), никогда по имени
или научному названию — иначе два разных вида с разными находками молча
склеятся в один при импорте.

**Совместимость:** старый архив (без `categories/`) читается новой версией как
сейчас — папки просто нет, ветка пропускается. Новый архив в старой версии
приложения уже отсекается существующей проверкой
`require(manifest.schemaVersion <= EXPORT_SCHEMA_VERSION)` — поднять
`EXPORT_SCHEMA_VERSION` до 2. Сбой на секции категорий не должен ронять импорт
целиком: та же логика, что с прогулками — не удалось, значит находки этого вида
уедут в `category_misc`, как и до фичи.

**Насколько это усложняет логику:** ~100–130 строк на обе стороны плюс один DTO,
новых механизмов ноль — `ZipWriter`/`ZipReader` уже умеют произвольные записи,
копирование файла в хранилище один в один повторяет существующий
`ImportDataUseCase.copyPhoto`. Реальная цена не в коде, а в тестировании:
матрица из трёх случаев слияния выше плюс повторный импорт того же архива — это
то, что нужно прогнать руками, а не «должно работать».

## Решение по зависимостям (подтверждено — ноль новых)

**Ноль новых зависимостей** (решено владельцем): Coil 3, okio, kotlinx.serialization,
Compose Canvas уже в `libs.versions.toml`, а галерея и PNG-кодирование — это
~40 строк на платформу тем же `expect`/`actual` паттерном, что уже применён для
камеры/фото. Альтернатива по правилу «кроссплатформенная библиотека — первый
вариант» (FileKit / peekaboo на пикер галереи) рассмотрена и отклонена:
экономия ~80 строк не стоит новой зависимости в supply-chain аудите перед
релизом (навык `pre-release-audit`) при уже существующем в проекте идентичном
платформенном слое.

Автоудаление фона (ML) в этот план сознательно не входит: iOS 17+ умеет
бесплатно через Vision, на Android аналог — ML Kit Subject Segmentation
(единицы МБ bundled, либо GMS-вариант с докачкой модели и неработающий без Google
Play). Разный вес, разное поведение, платная в размере фича. Опционально —
Phase 8, поверх готового ручного ластика, схему не трогает.

## Mac vs Windows

В отличие от `mushroom-collections.md`, здесь `expect`/`actual` есть, и
существенный: Phase 2 (пикер галереи + PNG-кодирование) пишет реальный `iosMain`
код, который на Windows-машине **не компилируется даже формально** —
`:shared:compileKotlinIosSimulatorArm64` там `SKIPPED` (cinterop MapLibre
недоступен без Xcode, известное ограничение). Варианты: писать Phase 2 сразу на
Mac, либо писать на Windows «вслепую» и закладывать, что первая же сборка на Mac
в Phase 7 вскроет опечатки в K/N API. Остальные фазы (0, 1, 3, 4, 5, 6) — чистый
commonMain, машина не важна.

## Фазы

- [x] **Phase 0 — Схема + домен.** Room v5→v6: `MIGRATION_5_6`, четыре
  аддитивные колонки (`source TEXT NOT NULL DEFAULT 'APP'`, `customNames`,
  `scientificName`, `iconFile`), enum `CategorySource` + пара методов в
  `Converters` (по образцу `EdibilityStatus`), обновить
  `CategoryEntity`/`Category`/маппинги. `CategoryDao`/`CategoryRepository` —
  `observeBySource(...)`/`observeUserCreated()` (`WHERE source != 'APP'`) для
  экрана «Мои грибы» и для экспорта. **Метода удаления не заводить вообще** —
  см. раздел про скрытие; скрытие целиком лежит на существующем
  `SetCategoryPickedUseCase`. Проверить, что `EnsureDefaultCategoriesUseCase`
  действительно не трогает не-`APP` строки, и что `upsert` идёт через настоящий
  `@Update` (грабля Phase 1 прошлого плана: старый `INSERT OR REPLACE` сносил бы
  `category_collections` своего вида каскадом). Без UI.

  Сделано как задумано, с двумя уточнениями по ходу: `customNames` — не
  nullable-колонка, а `NOT NULL DEFAULT '{}'` (иначе у поля два разных пустых
  состояния, `null` и `{}`), и хранится через **`Converters`** как
  `Map<AppLanguage, String>` прямо в `CategoryEntity` — тем же способом, что уже
  лежит `EdibilityStatus`, а не разбором JSON в репозитории. Ключи JSON —
  `AppLanguage.code` (`ru`/`en`), неизвестный код при чтении отбрасывается, а не
  роняет запрос. `upsert` уже был настоящим `@Update` (починен в прошлом плане),
  трогать не пришлось; `EnsureDefaultCategoriesUseCase` не пришлось менять
  вообще — он ищет строки по каталожным `nameKey`, а diff сравнивает целые
  `Category`, так что новые поля попали в сравнение сами.

  Проверено: `:shared:compileAndroidMain`, `:shared:compileKotlinIosSimulatorArm64`
  и `:shared:testAndroidHostTest` — зелёные; экспортированная `6.json` (13
  колонок) сверена с `MIGRATION_5_6` **прогоном на живом SQLite**: v5-таблица из
  `5.json` + четыре `ALTER` дают ровно ожидаемый Room набор колонок (имя,
  affinity, notNull), лишних нет, а строка, вставленная до миграции, получает
  `source='APP'`/`customNames='{}'`, а не `NULL`.
  `:shared:compileCommonMainKotlinMetadata` падает — **и падал до этой фазы
  тоже** (11 ошибок на чистом дереве, все про okio: `FileSystem.SYSTEM`,
  `readUtf8`/`writeUtf8` в `MapStyleCacheRepository`/`Export`/`Import`/
  `RepairPhotoPathsUseCase`). Задача сломалась когда-то после Phase 0 прошлого
  плана; к этой фазе отношения не имеет, но как быстрая проверка common-кода
  больше не годится — использовать iOS-компиляцию.

  Осталось на пользователя: установка APK **поверх существующей БД** (реальная
  миграция v5→v6 на живых данных, не с нуля) — приложение должно открыться,
  каталог и уже собранные находки остаться на месте.

- [x] **Phase 1 — Резолвер имени и иконки.** `CategoryIcon` + перегрузки
  `categoryDisplayName(category, ...)`; перевести все шесть мест обращения к
  `Res.allDrawableResources` и `CategorySorting.kt`. Маркер карты — по паттерну
  `PlaceMarkerIcon.kt` (`State.Success` + `disallowHardwareBitmaps()`).
  Всё ещё без экрана создания: тестовый пользовательский вид заводится разово
  прямым `INSERT` в БД (или временной debug-кнопкой), чтобы фаза была проверяема
  сама по себе. Каталожные виды после этой фазы обязаны выглядеть **точно так
  же, как до неё** — это чистый рефакторинг для них.

  Сделано с одним добавлением к плану: между `Category` и обоими резолверами
  появился **`CategoryIconSource`** (`domain/model/`, sealed `Bundled(iconRef)` /
  `UserFile(fileName)` + `Category.iconSource()`). Он понадобился потому, что
  `MapMarker` группирует находки по иконке и строит из неё id слоёв MapLibre —
  голого `iconRef: String?` для этого больше не хватает, а тащить в `ui/map`
  композабл `CategoryIcon` нельзя (MapLibre нужен `Painter`). Поэтому в
  `MapMarker` теперь `icon: CategoryIconSource?`, а id слоёв — из `icon.key`
  (`file_<fileName>` для своих). Тот же тип чинит слой: `SettingsViewModel`
  фильтрует превью-вид через `iconSource()`, не зная ничего про UI.
  Точек резолва иконки осталось ровно две — `CategoryIcon` (все пять
  UI-мест) и `rememberMushroomMarkerPainter` (обе карты).

  `categoryDisplayName(category)` при пустом `customNames[language]` падает на
  второй язык, потом на `scientificName` и только потом на `nameKey` — вид,
  названный только по-русски, не должен превращаться в `user_...` при
  переключении языка (заметка добавлена в `i18n/CLAUDE.md`).

  Временная debug-обвязка (**удалить в Phase 4**): `DebugUserCategoryUseCase` +
  кнопка внизу Настроек + 3 `StringKey`. Первое нажатие создаёт единственный
  `USER`-вид `user_debug_sample` (фикс. `nameKey` — чтобы повторные нажатия не
  плодили близнецов; настоящая схема `user_<millis>_<random>` — в Phase 4) с
  картинкой, скопированной из каталожного webp, дальше кнопка скрывает/
  возвращает его (`SetCategoryPickedUseCase`, возврат — с явным
  `isActive = true`). Заодно это ранняя живая проверка сценария скрытия.

  Проверено: `:shared:compileAndroidMain`,
  `:shared:compileKotlinIosSimulatorArm64`, `:shared:testAndroidHostTest` —
  зелёные (предупреждения `Calling a MapLibre Composable...` — старые, см.
  `ui/map/CLAUDE.md`).

  Осталось на пользователя (Android, поверх существующей БД): каталожные виды
  везде выглядят **как раньше**; кнопка в Настройках создаёт «Тестовый вид» — он
  появляется плиткой на Записи (с картинкой и именем), маркером на Карте и на
  карте прогулки, строкой в диалоге Фильтра, карточкой в бублике деталей
  прогулки; переключение языка меняет его имя на «Test species»; повторное
  нажатие кнопки прячет вид отовсюду, третье — возвращает.

- [x] **Phase 2 — Платформенный слой картинки.** `expect`/`actual`: пикер
  галереи (Android `PickVisualMedia`, iOS `PHPickerViewController`),
  декодирование выбранного файла в `ImageBitmap` с даунскейлом до ≤1536 px,
  `encodePng`. Файл иконки пишется в хранилище через `PhotoStorage` + okio.
  Оба `actual` — рядом с `CameraLauncher.*`, тем же стилем.
  Мина iOS из корневого CLAUDE.md: делегат пикера — **`var` на классе, не
  локальный `val`**, иначе ARC освободит его и колбэк не придёт (та же грабля,
  что была с `CLLocationManager`).

  Сделано как задумано. Новое: `GalleryPicker.kt` (+ два `actual`),
  `ImageCodec.kt` (`decodeScaledImage`/`encodePng` + `actual`'ы),
  `ui/util/ImageScaling.kt` (`scaledToMaxDimension` — чистый commonMain на
  `Canvas(ImageBitmap)`, тот же механизм понадобится Phase 3 для финального
  рендера) и `SaveCategoryIconUseCase` (уже не временный код: пишет файл,
  проставляет `iconFile`, удаляет предыдущий).

  Три вещи, которых в плане не было и которые стоили времени:
  - **`NSItemProvider.loadObjectOfClass(UIImage)` из Kotlin/Native не
    вызывается** — в биндинге параметр типизирован как
    `NSItemProviderReadingProtocol`, класс-объект туда не подходит. Вместо него
    `loadFileRepresentationForTypeIdentifier("public.image")`; временный URL
    живёт только внутри хендлера. Подробности — `iosMain/CLAUDE.md`.
  - **EXIF-ориентация**: `BitmapFactory` (Android) и Skia (iOS) её игнорируют,
    фото с телефона декодировалось бы боком. Android — ручной поворот через
    `android.media.ExifInterface` (framework-класс, не новая зависимость), iOS —
    отрисовка через `UIImage`, который применяет ориентацию сам.
  - **Coil кэширует по строке модели**, поэтому имя файла иконки —
    `catimg_<nameKey>_<millis>.png` с удалением старого, а не перезапись по
    фиксированному пути (иначе новая картинка не видна до перезапуска). См.
    `data/CLAUDE.md`.

  Проверено: `:shared:compileAndroidMain`, `:shared:compileKotlinIosArm64`,
  `:shared:compileKotlinIosSimulatorArm64`,
  `:shared:linkDebugFrameworkIosSimulatorArm64` (линковка — чтобы убедиться, что
  `PhotosUI` реально резолвится, а не только компилируется),
  `:shared:testAndroidHostTest`, `:androidApp:assembleDebug` — зелёные.

  Временная проверка для пользователя (вторая debug-кнопка в Настройках, видна
  только когда тестовый вид создан; **удаляется в Phase 4 вместе с остальной
  обвязкой**): «Картинка тестового вида — из галереи» гоняет весь путь
  целиком — системный пикер → декод с даунскейлом до 1536 px и разворотом по
  EXIF → даунскейл до 400 px → PNG → файл → резолвер Phase 1. Ластика/кропа тут
  нет, это Phase 3.

- [ ] **Phase 3 — Редактор.** Экран-редактор поверх Phase 2: ластик
  (`BlendMode.Clear` в offscreen-слое), размер кисти, undo/redo, прямоугольный
  кроп, шахматка прозрачности, кнопка «Готово» → рендер в `ImageBitmap` 400 px →
  `encodePng` → файл. Изолирован: на вход путь к исходному фото, на выход имя
  файла готовой иконки, ничего не знает про `Category`.

- [x] **Phase 4 — Раздел «Грибы» + форма создания/редактирования.** Решение
  владельца от 2026-08-19 уточнено в тот же день: точка входа — не голая
  кнопка «Добавить гриб», а полноценный top-level раздел **«Грибы»**
  (`Destination.Species`, `ui/screens/SpeciesScreen.kt`), объединяющий то, что
  раньше было раскидано по разным местам — пикер подборок **переехал сюда из
  `SettingsScreen`** (был там с `mushroom-collections.md`), и список «Мои
  грибы» с формой создания. Кнопка на `HomeScreen` — после «Карты», не самой
  нижней (группа «контент»: Запись/Архив/Карта/Грибы, отдельно от
  Подготовка/Настройки/Данные). Переход — `navigateToTopLevel()`, как и все
  top-level разделы (`ui/navigation/CLAUDE.md`).

  **Два входа в форму, не один** — второе уточнение того же дня: помимо
  кнопки «Добавить гриб» на экране «Грибы», самая правая, постоянная плашка
  в ленте `RecordScreen` (`AddSpeciesTile`, `ui/components/MushroomTile.kt`)
  открывает ровно ту же форму **как диалог поверх экрана записи**, не уводя
  никуда — активная прогулка не прерывается. `SpeciesFormDialog`
  (`ui/components/SpeciesFormDialog.kt`) — один общий composable для обоих
  входов и для правки существующего вида, тем же паттерном оверлея, что
  `MushroomSearchDialog`/`MushroomBulkAddDialog`. После сохранения со
  «Записи» — `RecordViewModel.saveNewSpecies` зовёт уже существующий
  `bringCategoryToFront`, новая плитка сама уезжает в начало ленты; никакая
  находка не логируется автоматически, пользователь сам жмёт «+».

  **Форма проще исходного текста плана** — третье уточнение того же дня:
  вместо двух полей имени (по языку) — одно поле «Название» (только текущий
  язык, обязательное) и одно необязательное поле «Научное название». Если
  вид открыт на языке без своего имени, показ идёт через уже работающий
  фолбэк `categoryDisplayName(category)` на `scientificName` — второе имя
  форма никогда не собирала бы, так что упрощение не потребовало трогать
  резолвер. Если научное название не заполнено — подставляется автоматически
  (`scientificNameFallback`, `domain/util/Transliteration.kt`): для русского
  имени — практическая транслитерация в латиницу, для английского — то же
  самое имя как есть. Итог: у пользовательского вида `scientificName`
  практически никогда не `null`. **Это не новый ключ слияния при импорте** —
  Phase 6 обязана продолжать сверять виды по `nameKey`, не по видимому имени
  (см. её раздел ниже) — два разных вида легко получают одинаковое научное
  название именно из-за этого автозаполнения.

  Съедобность/цвет — как и планировалось (`EdibilityStatus`, палитра свотчей
  — её ни для чего в проекте раньше не было, завели `SPECIES_COLOR_PALETTE`
  прямо в `SpeciesFormDialog.kt`, 12 значений). Иконка — камера/галерея, тот
  же пайплайн, что временная debug-кнопка (`decodeScaledImage` →
  `encodePng` → `SaveCategoryIconUseCase`), **без ластика** — Phase 3
  пропущена, фото сохраняется как есть, без выреза фона; поле необязательное,
  `CategoryIcon` рендерит вид без иконки как пустое место, макет не ломается.
  Ластик — будущая доработка того же диалога, ничего не потребуется
  переделывать.

  Список «Мои грибы» — по `source` (не по `isActive`/`isPicked`, иначе
  скрытый вид станет недостижим), `USER` выше `IMPORTED`, у импортированных —
  подпись «из архива»; редактировать можно и те и другие (`source` —
  происхождение, не право доступа, при правке не меняется). Тумблер
  «показывать» — `ToggleUserSpeciesVisibilityUseCase`, новый тонкий враппер
  над `SetCategoryPickedUseCase`, **обе стороны пишут `isActive` явно**
  (см. раздел про скрытие выше) — та же логика, что раньше была в
  `DebugUserCategoryUseCase.invoke`, теперь на постоянной основе.

  Удалена вся временная debug-обвязка Phase 1/2: `DebugUserCategoryUseCase`,
  обе кнопки внизу `SettingsScreen`, четыре ключа `SettingsDebugUserCategory*`.
  `CollectionPicker`-состояние (`collectionPickerItems`/`toggleCollection`/
  `setCategoryPicked`) тоже ушло из `SettingsViewModel` — теперь целиком в
  новом `SpeciesViewModel`, который повторяет тот же идемпотентный
  `ensureDefaultCategories()`/`ensureDefaultCollections()`/
  `recalculateFilterEligibility()` в `init`, что уже был у `RecordViewModel`/
  `SettingsViewModel` (раздел «Грибы» тоже может быть первым открытым
  экраном).

- [ ] **Phase 5 — Интеграция в существующие экраны.** Проверить и довести:
  плитка на Записи (в т.ч. поиск по имени и «+»/«−»), маркер на Карте,
  диалог Фильтра, `MushroomDonutChart` и легенда в деталях прогулки, пикер
  подборок (свои виды — вне подборок, показывать отдельной группой либо не
  показывать вовсе, решить здесь), `RecalculateFilterEligibilityUseCase` (свой
  вид с `isPicked=true` не должен гаситься каскадом `isActive`).

- [ ] **Phase 6 — Экспорт/импорт.** Сейчас категории в архив **не попадают
  вообще** (см. доккоммент `ExportDataUseCase`: каталог фиксирован и
  пересоздаётся на каждой установке), а неизвестный `categoryNameKey` при
  импорте молча уходит в `category_misc` — то есть без этой фазы экспорт/импорт
  тихо уничтожает все пользовательские виды и обезличивает их находки.
  Реализовать раскладку и правила слияния из раздела «Экспорт/импорт своих
  видов»: папка `categories/` (JSON строк с `source != APP`, ограниченных теми
  видами, что встретились в отобранных прогулках, + их иконки),
  `EXPORT_SCHEMA_VERSION` → 2, на импорте — создание/дополнение по `nameKey`
  **до** разбора прогулок, новые строки как `IMPORTED`.
  Прогнать руками всю матрицу слияния: (а) вида нет → создан с картинкой и
  находки к нему привязались; (б) вид есть без картинки → картинка подтянулась,
  имя не перезаписалось; (в) вид есть с картинкой → не изменилось ничего;
  (г) **повторный импорт того же архива** → прогулки продублировались (штатно),
  виды — нет; (д) старый архив без `categories/` читается как раньше.

- [ ] **Phase 7 — iOS/визуальный проход.** Mac-сеанс: пикер галереи, редактор
  (жесты и `BlendMode.Clear` на Skia-бэкенде iOS), экран «Мои грибы», свой вид
  как маркер на карте, экспорт/импорт архива со своими видами. Заодно — заметки
  по неочевидному в `data/CLAUDE.md` (v6, `iconFile` vs `photoPath`),
  `ui/map/CLAUDE.md` (свой маркер), `iosMain/CLAUDE.md` (делегат пикера).
  **Финальная проверка на реальном устройстве — за пользователем** (см.
  `feedback_no_self_testing`); симуляторный проход её не заменяет.

- [ ] **Phase 8 — опционально, позже.** Автоудаление фона: iOS Vision
  (`VNGenerateForegroundInstanceMaskRequest`, iOS 17+, бесплатно по размеру),
  Android ML Kit Subject Segmentation (взвесить GMS-вариант против bundled).
  Кнопкой «убрать фон автоматически» поверх ручного ластика, с ручной доводкой
  после — то есть Phase 3 остаётся рабочим фолбэком там, где ML недоступен.

## Что проверять живьём (для каждой фазы, коротко)

Установка **поверх существующей БД**, не с нуля — миграция v5→v6 должна пройти
на реальных данных. Отдельно: свой вид с находками пережил перезапуск процесса;
скрытый вид не исчез из «Моих грибов» и вернулся обратно на Запись/Карту;
экспорт → переустановка → импорт вернул свои виды **с картинками**, а не в
«Прочее»; повторный импорт того же архива не удвоил виды.
