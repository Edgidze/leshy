# ============ Gradle / Kotlin ============
.gradle/
build/
**/build/
!src/**/build/
kotlin-js-store/
node_modules/
.kotlin/

# ============ Android ============
local.properties
*.jks
*.keystore
keystore.properties
*.apk
*.aab
*.ap_
*.dex
*.class
captures/
.externalNativeBuild/
.cxx/
/release/

# ============ IntelliJ / Android Studio ============
.idea/
*.iml
*.ipr
*.iws
out/

# ============ iOS / Xcode ============
xcuserdata/
**/xcuserdata/
**/*.xcuserstate
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# ============ CocoaPods (если появится) ============
Podfile.lock

# ============ SPM for KMP (плагин spmForKmp, iOS-зависимости) ============
shared/src/swift/
**/spmKmpPlugin/

# ============ macOS / OS ============
.DS_Store
Thumbs.db

# ============ Claude Code (личные настройки, не для репозитория) ============
.claude/settings.local.json

# ============ Рабочие документы владельца, не часть проекта ============
# Список задач, по которому шла работа: лежит в корне проекта для удобства, но
# репозиторию не принадлежит. Без этой строки любой `git add -A` затягивает его обратно.
Tasks plan.rtf

# ============ Python для скриптов tools/ ============
# Babel (названия стран из CLDR) и кеш скачанных колёс: живут в проекте, чтобы
# не выкачивать 10 МБ заново при каждой новой стране, но репозиторию не
# принадлежат. Ставится одной командой: python3 tools/pydeps.py --fetch
tools/.pydeps/

# ============ Прочее ============
*.log
*.hprof
.env
