# CLAUDE.md — «Леший: карта грибов» / "Leshy: mushrooms map"

> Этот файл Claude Code читает автоматически при старте сессии.
> Он задаёт контекст проекта, архитектуру и правила. Держи его в актуальном
> состоянии: при завершении каждой части обновляй раздел «Статус».

---

## 1. Что это за приложение

Мобильное приложение для записи «тихой охоты» — походов в лес за грибами.
Концептуально похоже на Strava (запись трека тренировки), но главная ценность —
**история найденных грибов**: где, когда, какого типа и сколько.

Три основных раздела:
- **Запись / Record** — запись новой прогулки (трек + отметки грибов/ориентиров + фото)
- **Архив / Archive** — лента прошлых прогулок с детализацией каждой
- **Карта / Map** — агрегированная карта всех находок и маршрутов + статистика

Полное ТЗ — в `SPEC.md`. При любых расхождениях `SPEC.md` — источник истины по
функционалу, этот файл — по архитектуре и правилам разработки.

---

## 2. Технологический стек (зафиксирован — не менять без явного запроса)

| Область | Выбор |
|---|---|
| Платформы | Android + iOS |
| Язык / UI | Kotlin Multiplatform + Compose Multiplatform |
| Карты | **MapLibre + OSM** (оффлайн-тайлы, без API-ключей) |
| Навигация | Compose Navigation (официальная от JetBrains, KMP) |
| Асинхронность | Kotlin Coroutines + Flow |
| DI | Koin |
| Локальная БД | Room (KMP), миграции от версии 1 |
| Архитектура | MVVM + слой domain, вся логика и UI в `shared` |

---

## 3. Структура проекта

```
leshy/
├── shared/                              # ВЕСЬ общий код: логика + UI
│   └── src/
│       ├── commonMain/kotlin/.../
│       │   ├── data/
│       │   │   ├── local/               # Room: entities, DAO, database, миграции
│       │   │   ├── repository/          # реализации репозиториев
│       │   │   └── platform/            # expect-объявления (GPS, камера, карта)
│       │   ├── domain/
│       │   │   ├── model/               # чистые доменные модели
│       │   │   ├── repository/          # интерфейсы репозиториев
│       │   │   └── usecase/             # use-cases
│       │   ├── presentation/            # ViewModel'и + UiState на экран
│       │   ├── ui/
│       │   │   ├── screens/             # Compose-экраны (record/archive/map/settings)
│       │   │   ├── components/          # переиспользуемые компоненты (плашка гриба и т.п.)
│       │   │   ├── navigation/          # NavHost, маршруты
│       │   │   └── theme/               # тема, цвета, типографика
│       │   ├── i18n/                    # строки RU/EN, механизм локализации
│       │   └── di/                      # Koin-модули
│       ├── androidMain/kotlin/.../      # actual: Room-драйвер, GPS, камера, MapLibre Android
│       └── iosMain/kotlin/.../          # actual: то же для iOS
├── androidApp/                          # тонкий Android-хост (MainActivity)
├── iosApp/                              # тонкий iOS-хост (SwiftUI-обёртка)
├── CLAUDE.md                            # этот файл
├── SPEC.md                              # полное ТЗ
└── .gitignore
```

**Принцип:** `androidApp` и `iosApp` максимально тонкие. Всё, что можно вынести
в `shared` — выносится в `shared`. Платформенное (GPS, камера, БД-драйвер,
рендер карты) прячется за `expect`/`actual`.

---

## 4. Модель данных (Room, 4 таблицы, версия 2)

Все таблицы создаются в схеме версии 1. С самого начала используем явные
миграции — **никакого `fallbackToDestructiveMigration`**.

### `categories` — категории отмечаемых объектов (в основном грибы)
- `id` (PK)
- `nameKey` — ключ локализации, НЕ готовая строка (для мультиязычности)
- `colorHex` — цвет маркера/точки/контура плашки
- `iconRef` — имя ресурса в `composeResources/drawable` (без расширения),
  nullable — используется через `Res.allDrawableResources[iconRef]`
  (динамический lookup по строке, т.к. `Res.drawable.xxx` — статические
  accessor'ы, не подходят для рантайм-выбора из 30 видов)
- `order` — порядок в интерфейсе записи
- `isActive` — показывать ли плашку сейчас (редактируется в настройках)
- `edibilityStatus` (с версии 2) — `EdibilityStatus`: `EDIBLE`/
  `CONDITIONALLY_EDIBLE`/`INEDIBLE`, определяет цвет значка съедобности на
  плашке (зелёный/жёлтый/красный)

### `walks` — прогулки
- `id` (PK)
- `name` — название прогулки (вводит пользователь)
- `startTime`, `endTime`
- `distanceMeters`
- `avgSpeed`
- `startLat`, `startLon`, `endLat`, `endLon`
- `mushroomCount` — денормализованный счётчик для быстрой ленты архива

### `objects` — найденные объекты (грибы / фото / ориентиры)
- `id` (PK)
- `walkId` (FK → walks)
- `categoryId` (FK → categories)
- `lat`, `lon`
- `timestamp`
- `type` — enum: MUSHROOM / PHOTO / POI
- `photoPath` — nullable, путь к фото (для type=PHOTO)

### `track_points` — точки трека (по аналогии со спортивными трекерами)
- `id` (PK)
- `walkId` (FK → walks)
- `lat`, `lon`
- `timestamp`
- `elevation` — nullable
- `sequence` — порядковый номер точки в треке

**FK с `onDelete = CASCADE`**: удаление прогулки чистит её объекты и точки трека.

---

## 5. Критичные правила разработки (из ТЗ)

1. **Запись каждого find сразу в БД.** При нажатии «+» тип/место/время гриба
   немедленно коммитятся в Room. При «−» — удаляется последняя отметка этого
   типа. НЕ буферизировать в памяти: если запись прервётся (сбой, закрытие),
   всё успевшее записаться должно сохраниться без планового завершения прогулки.

2. **Локализация через ключи.** Никаких хардкод-строк в UI. Все тексты — через
   механизм i18n (RU + EN минимум, с возможностью добавить языки). Названия
   категорий тоже локализуются через `nameKey`.

3. **Миграции БД с v1.** Структуру закладываем с расчётом на будущие изменения.
   Явные `Migration`, экспорт схемы включён.

4. **Вся логика и UI в `shared`.** Платформенный код — только за `expect`/`actual`.

5. **Иммутабельный UiState + StateFlow** на каждый экран. ViewModel не держит
   Android-зависимостей (общий KMP ViewModel).

6. **Лучшие практики.** Единый репозиторий как точка доступа к данным,
   use-cases для бизнес-логики, DI через Koin, никаких утечек контекста.

---

## 6. План работ (порционно-модульно)

Двигаемся частями. Каждую часть: написать → собрать (`./gradlew build`) →
починить ошибки → показать → зафиксировать в git → следующая.

- [x] **Часть 1. Скелет + модель данных.** Структура KMP-проекта, Gradle
      (version catalog), Koin-каркас, Room (entities, DAO, database, миграция v1),
      репозитории, доменные модели, i18n-каркас RU/EN. Приложение запускается
      с пустым NavHost и 4 заглушками экранов.
- [x] **Часть 2. Раздел «Запись».** Плашки грибов (+/счётчик/−), плашка камеры,
      старт/пауза/финиш, GPS-трекинг (expect/actual), live-карта MapLibre с треком
      и маркерами, запись каждого события в БД.
- [x] **Часть 3. Раздел «Архив».** Лента карточек прогулок, детальный экран
      с картой, треком и маркерами конкретной прогулки.
- [x] **Часть 4. Раздел «Карта».** Агрегированная карта всех находок (приоритет —
      места грибов, «тепловая карта») + все маршруты. Статистика с фильтром по
      месяцам/годам. Переключение статистика/карта.
- [x] **Часть 5. Настройки + логотип.** App Bar (слева название, справа шестерёнка),
      выбор языка, редактирование набора активных грибов. SVG-логотип лешего.

---

## 7. Статус

**Части 1–3 сделаны.** Скелет KMP-проекта, полная Room-модель, репозитории,
Koin, навигация — разделы «Запись» (плашки грибов, камера, старт/пауза/финиш,
GPS-трекинг, live-карта текущей прогулки) и «Архив» (лента карточек прогулок,
детальный экран с разбивкой находок по категориям, карта конкретной прогулки).
`./gradlew build` проходит чисто на Android + iOS.

**Часть 4 сделана.** Реальная карта MapLibre теперь работает и на iOS (не только
на Android), плюс агрегированная карта находок/маршрутов и статистика с фильтром
по периоду — см. решения ниже.

**Часть 5 сделана.** Экран «Настройки»: выбор языка (мгновенное переключение
внутри приложения, без перезапуска) и включение/выключение грибов из набора для
отметки; SVG-логотип лешего — как изображение на экране «Настройки» и как
launcher icon на Android + iOS.

Решения, принятые в Части 1:
- Пакет оставлен как есть (`compose.project.leshy`, сгенерирован визардом) —
  переименование не запрашивалось.
- Домен-модель для таблицы `objects` называется `FieldMark` (тип `MarkType`),
  т.к. слово `object` зарезервировано в Kotlin; Room-сущность — `ObjectEntity`.
- Room 2.8.4 (пакет `androidx.room`), а не Room 3.0 (`androidx.room3`) — 3.0
  вышла совсем недавно, решили не рисковать на самом старте проекта.
- Локализация — через встроенный механизм Compose Multiplatform resources
  (`commonMain/composeResources/values{,-ru}/strings.xml`, `Res.string.*`),
  без сторонних библиотек (moko-resources и т.п.).
- DI границы БД: `expect val platformModule: Module` отдаёт
  `RoomDatabase.Builder<LeshyDatabase>` (Android — через `androidContext()` +
  `getDatabasePath`, iOS — через `NSFileManager` documents dir); общий
  `dataModule` строит БД/DAO/репозитории поверх него.

Решения, принятые в Части 2:
- **MapLibre: реальная карта только на Android, iOS — заглушка.** На этой
  машине нет Xcode/CocoaPods, а `maplibre-compose` тянет нативный MapLibre SDK
  через CocoaPods на iOS-таргете — без Mac эту связку не собрать и не
  проверить. По явному выбору пользователя: `maplibre-compose` подключена
  только в `androidMain.dependencies` (в commonMain её нет вообще), поэтому
  iOS-сборка её не касается. `LiveTrackMap` — `expect/actual @Composable`:
  Android рисует реальную карту (MapLibre + OSM-растровые тайлы без ключей,
  `BaseStyle.Empty` + свой `RasterLayer`, трек как `LineLayer` по `GeoJsonData`,
  находки как `CircleLayer` per цвет категории), iOS — плейсхолдер с текстом
  «появится после сборки через Xcode/CocoaPods». Когда дойдём до Части 4
  (агрегированная карта), нужно будет либо получить доступ к Mac для iOS, либо
  ещё раз явно решить с пользователем, что делать с iOS-картой.
- **Категории засеяны сейчас** (`EnsureDefaultCategoriesUseCase`): 4 гриба
  (белый/лисичка/рыжик/подберёзовик) + служебная неактивная категория
  `category_misc`, на которую вешаются отметки PHOTO/POI (нужна валидная
  FK-ссылка, т.к. `objects.categoryId` не nullable). Список грибов — это
  просто первичный набор; менять/расширять его будет Часть 5 (настройки).
- **GPS** — `LocationTracker` интерфейс + expect/actual через Koin (как БД в
  Части 1). Android: `android.location.LocationManager` напрямую (без
  Google Play Services/Fused Location, чтобы не тянуть лишнюю зависимость).
  iOS: `CLLocationManager` + делегат. Разрешения: Android запрашивает
  `ACCESS_FINE_LOCATION`+`CAMERA` в `MainActivity.onCreate` (единственное
  место, где разрешениям правда нужен Activity); iOS запрашивает через
  системный prompt при первом вызове (авто, никакого доп. кода не нужно).
  Если разрешение не выдано — трек просто не пишется, приложение не падает.
- **Камера** — `expect @Composable fun rememberCameraLauncher`. Android:
  `ActivityResultContracts.TakePicture()` + `FileProvider` (файлы в
  `filesDir/photos`). iOS: `UIImagePickerController` + сохранение в Documents.
- **Use-cases добавлены только сейчас** (Часть 1 их не заводила — там не было
  бизнес-логики). `domain/usecase`: старт/финиш прогулки, запись точки трека
  (+ пересчёт `distanceMeters` по haversine), добавление/удаление отметки
  гриба, добавление фото-отметки, засев дефолтных категорий.
- **RecordViewModel общий между экранами «Запись» и «карта текущей прогулки»**:
  оба `composable<>` в `LeshyNavHost` берут `koinViewModel` с одним и тем же
  `viewModelStoreOwner` (backstack entry экрана `Record`), иначе переход на
  карту создавал бы новый ViewModel и терял активную запись.
- Таймер длительности прогулки — тикает по 1 секунде, не пересчитывается от
  системных часов; расстояние — по реальным дельтам GPS-точек (haversine),
  поэтому не идёт во время паузы.

Решения, принятые в Части 3:
- Добавлена `kotlinx-datetime` (0.8.0) — понадобилась для форматирования
  старта/финиша прогулки (`LocalDateTime.Format` DSL); до этого в проекте
  обходились без неё (Long-таймстемпы + ручной тикер).
- `walks.mushroomCount` (денормализованный счётчик из Части 1) используется
  как есть в карточке ленты — не считаем находки заново из `objects`. На
  детальном экране разбивка «по типам» уже требует агрегации `objects` по
  `categoryId` (только type=MUSHROOM) — это делает `WalkDetailViewModel` через
  `combine()` четырёх Flow (walk/marks/track/categories).
- `WalkMapScreen` переиспользует тот же `expect/actual LiveTrackMap`, что и
  live-карта Части 2, просто с `currentLocation = null` (историческая
  прогулка). Экран «Архив» → «Детали» → «Карта» — тот же паттерн общего
  `koinViewModel` через `viewModelStoreOwner`, что и Record/RecordMap.
- Прогулка без `endTime` (не была явно завершена, напр. после сбоя) не теряет
  данные и всё равно отображается в ленте — длительность в этом случае
  показывается как «—», а не как ошибка.

Решения, принятые в начале Части 4 (iOS-карта):
- **Выбор пользователя: Swift Package Manager вместо CocoaPods**, хотя
  просили именно CocoaPods. Причина: у `maplibre-compose` 0.13.0 в доке
  заявлены оба варианта, но собственное демо-приложение и тесты библиотеки
  используют SPM (плагин `io.github.frankois944.spmForKmp`), плюс CocoaPods
  прекращает поддержку новых версий пакетов в конце 2026 — SPM надёжнее на
  практике. Подтверждено пользователем явным вопросом.
- Подключено через `shared/build.gradle.kts`: плагин `spmForKmp` (версия
  1.9.1 — та же, что использует сам `maplibre-compose` в релизе 0.13.0),
  `iosTarget.swiftPackageConfig { dependency { remotePackageVersion(...) } }`
  тянет `maplibre-gl-native-distribution` (версия `6.25.1`, тоже пиновка
  из `maplibre-compose` v0.13.0), плюс ручные `linkerOpts("-F...", "-rpath", ...)`
  на `binaries.all`, указывающие на scratch-каталог плагина — без этого
  Kotlin/Native framework не находит собранный `MapLibre.xcframework`.
  `gradle.properties`: добавлен `kotlin.mpp.enableCInteropCommonization=true`
  (требование плагина). В `iosApp.xcodeproj` build settings (Debug + Release)
  добавлен `OTHER_LDFLAGS = (-framework Shared, -framework MapLibre)` — порядок
  важен (Kotlin-фреймворк первым), иначе ломается рендеринг текста Compose
  на iOS из-за конфликта символов HarfBuzz (см. предупреждение самой
  библиотеки/JetBrains issue CMP-8882).
- **`LiveTrackMap` перестал быть `expect`/`actual`.** Раз maplibre-compose API
  идентичен на Android и iOS, реализация (Часть 2) перенесена целиком в
  `commonMain` как обычный `@Composable`; платформенные `.android.kt`/`.ios.kt`
  удалены, как и строка-заглушка `record_map_placeholder_ios`. Зависимость
  `maplibre-compose` тоже переехала из `androidMain` в `commonMain`.
- Проверено полностью, включая визуальный рендер: `./gradlew :shared:compileKotlinIosArm64`,
  `:shared:compileKotlinIosSimulatorArm64` и `:shared:compileAndroidMain` — все
  чисто; `xcodebuild` для `iosApp` (симулятор) — `BUILD SUCCEEDED`, линковка
  подхватывает `-framework Shared -framework MapLibre`; `./gradlew :androidApp:assembleDebug`
  тоже проходит. Приложение установлено и запущено в симуляторе (`iPhone 17`,
  iOS 26.5): экран «Запись» открывается без крашей, разрешение на геолокацию
  запрашивается корректно, а экран «Текущая прогулка» реально отрисовывает
  MapLibre-карту с OSM-тайлами и точкой местоположения — то есть то же самое,
  что на Android в Части 2, теперь работает и на iOS.

Решения, принятые в контенте Части 4 (агрегированная карта + статистика):
- **Данные:** `FieldMarkRepository`/`TrackPointRepository` получили `observeAll()`
  (+ `ObjectDao`/`TrackPointDao.observeAll()` без фильтра по `walkId`) — раньше
  оба репозитория умели отдавать данные только по конкретной прогулке.
- **`MapViewModel`** комбинирует 4 Flow (`walks`/`marks`/`trackPoints`/`categories`)
  в сырые данные, затем комбинирует их ещё с `mode` и `selectedPeriod` (свои
  `MutableStateFlow`) — фильтрация по периоду и агрегация статистики целиком
  в `buildUiState()`, без похода в БД повторно при смене фильтра/режима.
- **Период — `MapPeriod(year, month)`**, вычисляется из `walk.startTime` через
  `Instant.toLocalDateTime(TimeZone.currentSystemDefault()).month` (kotlinx-datetime
  0.8.0: `monthNumber` deprecated в пользу `month: Month`, а число месяца — через
  отдельно импортируемое extension-свойство `kotlinx.datetime.number`, не следует
  автоматически из `import kotlinx.datetime.*` на что можно наступить в будущем).
  Список доступных периодов — просто `distinct()` дат прогулок, без панели
  выбора года отдельно от месяца.
- **Подписи периодов — числовые «ММ.ГГГГ»** (`"07.2026"`), без отдельных
  локализованных названий месяцев — так короче на 12 новых строк RU/EN ради
  фильтра, который и так дополняется чекбоксом «За всё время».
- **Карта:** новый `AggregatedFindsMap` (`ui/map/`) — не переиспользует
  `LiveTrackMap` (та работает с одним треком), а рисует один `LineLayer` per
  прогулка (`tracks: Map<Long, List<GeoPoint>>`, приглушённый цвет/прозрачность)
  и один `HeatmapLayer` поверх — по находкам-грибам (`MultiPoint` в
  `GeoJsonData.Features`). Приоритет тепловой карты над маршрутами — по ТЗ.
  Камера один раз прыгает (`cameraState.jumpTo(BoundingBox(...))`) на bounding
  box всех точек трека+находок при первой загрузке данных.
- **Переключатель режимов** — `SingleChoiceSegmentedButtonRow`/`SegmentedButton`
  (Material3), фильтр периода — `FilterChip` в `LazyRow`. Статистика переиспользует
  вид `WalkDetailScreen` (список строк + разбивка по категориям через уже
  существующий `CategoryCount` из `presentation.archive`, чтобы не заводить
  второй одинаковый класс).
- Проверено вживую в симуляторе (iPhone 17, iOS 26.5) на тестовых данных из
  Части 2: тепловая карта реально рисуется поверх OSM-тайлов, переключение
  Карта↔Статистика и фильтр по периоду (「За всё время」↔「07.2026」) работают
  и дают согласованные цифры. Android: `./gradlew :androidApp:assembleDebug`
  проходит чисто (общий Compose-код, реальный запуск на Android-эмуляторе в
  этой сессии не делался — MapLibre на Android уже проверялся вживую в Части 2).

Решения, принятые в Части 5 (Настройки + логотип):
- **Хранение настроек — AndroidX DataStore (Preferences), мультиплатформенно**
  (`datastore-preferences-core` 1.2.1 + Okio), а не Room-таблица и не
  `multiplatform-settings`. Причина: DataStore — единственный из вариантов без
  ритуала «миграция схемы на каждую новую настройку» (ключевое при ожидаемом
  росте числа настроек) и без стороннего non-Google/JetBrains вендора (в
  Части 1 уже был отказ от сторонних либ ради встроенных механизмов — тот же
  принцип). Путь к файлу настроек — `expect/actual platformModule`, тем же
  паттерном, что и `RoomDatabase.Builder` (Android — `filesDir`, iOS —
  `NSFileManager` documents dir).
- **Локализация ПЕРЕСТАЛА идти через Compose Resources `Res.string`.** При
  реализации выяснилось (проверка исходников `components-resources-1.11.1`),
  что `ResourceEnvironment`/`LocalComposeEnvironment`/`LanguageQualifier` в этой
  версии библиотеки — `internal`/`@InternalResourceApi`, то есть переопределить
  локаль в рантайме из кода приложения невозможно; это отменяет решение по i18n
  из Части 1. По явному выбору пользователя заведён свой слой: `i18n/StringKey`
  (enum всех строк) + `i18n/Strings.kt` (`when`-таблицы RU/EN, exhaustive —
  компилятор ловит забытый перевод) + `LocalAppLanguage` (CompositionLocal) +
  composable `stringResource(key)`. Текущий язык хранится в DataStore и
  прокидывается один раз в `App()` через `CompositionLocalProvider`, поэтому
  переключение языка в «Настройках» применяется мгновенно во всём приложении
  без перезапуска. `values/strings.xml` и `values-ru/strings.xml` удалены
  целиком — единственный источник строк теперь `Strings.kt`. Для
  не-composable контекста (например, `RecordViewModel.start()`, который
  раньше звал suspend `getString(Res.string...)`) есть чистая функция
  `string(key, language)`; `RecordViewModel` держит текущий язык в обычной
  var, обновляемой через `collect` в `init` (тот же паттерн, что уже был для
  `categoryRepository.observeActive()`).
- **Набор активных грибов** — переиспользует существующий `isActive` на
  `Category` (никаких новых полей/таблиц); экран «Настройки» — список
  категорий с `Switch`, тумблер сразу пишет через `CategoryRepository.upsert`.
  Служебная неактивная категория `category_misc` (см. Часть 2, нужна для
  FK-ссылки PHOTO/POI-отметок) отфильтрована из списка в `SettingsViewModel` —
  это не гриб, который пользователь должен включать/выключать.
- **Логотип** — по выбору пользователя используется и внутри приложения
  (`composeResources/drawable/leshy_logo.svg`, `painterResource` на экране
  «Настройки»), и как launcher icon на обеих платформах. Рендер SVG→PNG
  сделан локально без интернет-инструментов (`rsvg-convert`/ImageMagick/
  Pillow — ничего не установлено): macOS `qlmanage -t` (Quick Look thumbnail
  generator) как SVG-рендерер + `sips` для ресайза. Три варианта SVG сделаны
  из оригинала: плоский квадрат без скругления (для iOS — Apple сам накладывает
  свою маску, поэтому исходник должен быть full-bleed), круг (Android
  `ic_launcher_round.*`, легаси/API<26), и только сама фигура лешего без фона
  прозрачным — под adaptive-icon foreground. Android adaptive icon: фон стал
  сплошной `#F4F1E8` (`drawable/ic_launcher_background.xml`, один `<path>`),
  foreground переведён с vector drawable на растровый `mipmap/ic_launcher_foreground.png`
  по плотностям (проще и надёжнее, чем вручную конвертировать SVG-обводки/дуги
  circle/ellipse в Android `pathData`). iOS: `app-icon-1024.png` обязан быть
  без alpha-канала (Apple отклоняет иконки с прозрачностью) — исходный рендер
  шёл с альфой, канал убран круглым провозом через JPEG (`sips -s format
  jpeg` → обратно в PNG), визуально не отличимо на растровом лого без
  градиентов.
- Проверено вживую в симуляторе (iPhone 17, iOS 26.5): мгновенное
  переключение RU↔EN во всём приложении (App Bar, нижняя навигация, «Настройки»,
  «Запись» — включая плашки грибов), сохранение выбора языка переживает
  полный перезапуск процесса (`simctl terminate` + `launch`), выключение
  гриба в «Настройках» тут же убирает его плашку на экране «Запись».
  Android: `./gradlew :androidApp:assembleDebug` и `./gradlew build` проходят
  чисто; реальный запуск на Android-эмуляторе в этой сессии не делался (нет
  настроенного AVD) — общий Compose-код тот же, что уже проверен вживую на
  Android в Части 2/4.
- **Найден и исправлен баг навигации** (не Android-специфичный — воспроизведён
  и надёжно отлажен на iOS-симуляторе, три итерации, пока не сошлось всё сразу):
  пока «Настройки» были заглушкой, никто не проверял переход с этого экрана
  обратно на нижнюю навигацию. С реальным контентом выяснилось — после ЛЮБОГО
  захода в «Настройки» кнопка `Record` в `NavigationBarItem` переставала
  работать ОТКУДА УГОДНО (не только напрямую из Settings, но и из Archive/Map
  после того, как Settings хоть раз посещались в этой сессии) — экран
  откатывался обратно на `Settings` вместо `Record`.
  - Итерация 1: `popUpTo(graph.findStartDestination().id)` →
    `popUpTo(graph.id)` — чинило только прямой случай Settings→Record, но не
    полный сценарий (Settings→Archive→Record всё ещё ломался).
  - Итерация 2: убрал `saveState`/`restoreState` целиком (`popUpTo(graph.id)
    { inclusive = true }` без save/restore) — почти правильно, но открыло ДРУГОЙ
    критичный баг: `RecordViewModel` привязан к `viewModelStoreOwner = backStackEntry`
    экрана `Record` (см. Часть 2), и `inclusive = true` уничтожает этот backstack
    entry при каждом уходе на другую вкладку — активная запись прогулки (таймер,
    флаг `isRecording`, накопленная дистанция) полностью сбрасывалась при
    переходе на Archive/Map/Settings и обратно, хотя сама прогулка в БД не
    терялась (только in-memory `RecordUiState`).
  - **Итог (правильная и итоговая версия):** корень обеих проблем —
    асимметрия: кнопка-шестерёнка делала на `Settings` голый `navigate()` без
    `popUpTo`, а нижняя навигация — с `popUpTo(findStartDestination){saveState=true}`
    + `restoreState=true`. Сохранённое-под-route-ключом состояние `Settings`
    (не входящего в нижнюю навигацию, но оказавшегося в том же бэкстеке)
    путало восстановление при последующих переходах. Исправлено — завёл общую
    `NavHostController.navigateToTopLevel(destination)` в `App.kt`, которой
    пользуются И шестерёнка, И все три пункта `NavigationBarItem`:
    `popUpTo(graph.findStartDestination().id) { inclusive = true; saveState = true }`
    + `launchSingleTop = true` + `restoreState = true`. Все четыре
    top-level-экрана (Record/Archive/Map/Settings) теперь проходят через ОДНУ
    и ту же схему pop/save/restore — никакой асимметрии, никакого
    рассинхрона в закэшированном состоянии.
  - Проверено вживую очень подробно, с активной запущенной прогулкой
    (таймер тикал, `Pause`-кнопка): Settings→Record/Archive/Map,
    Settings→Archive→Record (точный сценарий первого бага),
    Record→Map→Settings→Map→Record (полный цикл) — во всех случаях навигация
    работает корректно И состояние записи прогулки (таймер, флаг «идёт
    запись») переживает переключение вкладок без сброса.
- Android: реальный запуск на устройстве/эмуляторе в этой сессии так и не
  делался (нет подключённого девайса и настроенного AVD) — пользователь
  планирует настроить эмулятор отдельно. `./gradlew :androidApp:assembleDebug`
  после финального фикса навигации по-прежнему чистый.

**Баг найден и исправлен позже (первый реальный запуск на Android-эмуляторе,
эмуляторы `Medium_Phone`/`Resizable_Experimental` уже настроены пользователем):**
переход в «Настройки» крашил приложение на Android — `IllegalStateException:
Android platform doesn't support SVG format` (`SettingsScreen.kt:42`,
`painterResource(Res.drawable.leshy_logo)`). Причина — ограничение самой
библиотеки Compose Multiplatform Resources: декодирование SVG у неё
реализовано только через Skia (iOS/Desktop), на Android для растрового
`painterResource` рендерера SVG нет вообще (это отдельная проблема от
растеризации launcher-иконок в этой же Части 5 — там SVG растеризовали из-за
требований самих иконок, а не из-за этого ограничения; в прод-код напрямую
попал ещё и сырой SVG для показа логотипа в UI, и на Android его не
проверяли). Исправлено тем же способом, что и иконки: `leshy_logo.svg` →
растеризован в `leshy_logo.png` (512×512, `qlmanage -t` + `sips`, тот же
пайплайн) и подложен в `composeResources/drawable/` вместо svg — имя ресурса
(`Res.drawable.leshy_logo`) не меняется, код `SettingsScreen.kt` трогать не
пришлось. Проверено вживую на `Medium_Phone`: экран «Настройки» открывается
без крашей, логотип отображается, переключение языка и тумблеры грибов
работают; навигация Record↔Archive↔Map↔Settings проверена повторно — крашей
нет.

**Отдельно замечено при этом: полный `./gradlew build` на этой машине (8 ГБ
RAM) падает с `OutOfMemoryError: Java heap space` в `linkReleaseFrameworkIos*`**
(K2Native-компилятор линкует release-фреймворк iOS с полной оптимизацией —
памяти требует заметно больше, чем обычные `compileKotlinIosArm64`/
`iosSimulatorArm64`, которыми до сих пор и ограничивались проверки iOS).
Не связано с SVG-багом выше. Причина — не нехватка heap самой по себе, а
одновременная конкуренция за RAM с запущенным Android-эмулятором (qemu
занимает ~800 МБ) и параллельной линковкой сразу двух iOS-таргетов
(`iosArm64` + `iosSimulatorArm64`). **Решается точечно, без правки
`gradle.properties`:** остановить эмулятор (`adb emu kill`) и собирать с
`./gradlew build --max-workers=1` (не даёт двум линковкам идти одновременно).
Проверено — с этим `BUILD SUCCESSFUL` за ~4 минуты. Осознанно не стали
поднимать `-Xmx` в `gradle.properties` постоянно: это зарезервировало бы
лишнюю память под каждую обычную Android-сборку, а полная release-линковка
iOS в обычном цикле разработки не нужна — актуальна только перед реальной
сборкой iOS-релиза.

**Найдены и разобраны ещё два бага при первом реальном тестировании карт на
Android (эмулятор + физический Pixel 4a):**

1. **Краш при переходе на экран «Карта» (общая, агрегированная) — исправлен.**
   `IllegalArgumentException: Type of the key MapPeriod(...) is not supported.
   On Android you can only use types which can be stored inside the Bundle.`
   Причина: `MapScreen.kt` использовал `MapPeriod` (обычный `data class`, не
   `Parcelable`/`Serializable`) напрямую как `key` элемента `LazyRow` для
   фильтра периода — Compose требует, чтобы ключ можно было сохранить в
   Android `Bundle`. Исправлено заменой на примитивный `Int`-ключ
   (`it.year * 100 + it.month`).
2. **Размытая подложка карты (растровые OSM-тайлы) — исправлено переходом на
   OpenFreeMap.** Пользователь заметил зернистость/нечёткость подписей на
   карте (и на iOS-симуляторе, и на реальном Android) — маркеры находок и
   линия трека при этом чёткие. Причина: `tile.openstreetmap.org` отдаёт
   растровые PNG-тайлы 256×256 только в обычном (не retina/`@2x`) разрешении
   — на HiDPI-экране они масштабируются вверх, отсюда размытость именно на
   мелких деталях/тексте (маркеры и трек рисуются MapLibre как векторные
   слои поверх, в родном разрешении, поэтому не страдают). Дополнительный
   повод исправить — прямое хотлинк-использование `tile.openstreetmap.org` в
   реальном приложении формально нарушает их usage policy (рассчитана на
   лёгкое/тестовое использование). Решение — переход на бесплатный,
   безключевой векторный стиль **OpenFreeMap** (`ui/map/MapStyle.kt`,
   `BaseStyle.Uri("https://tiles.openfreemap.org/styles/liberty")`), тем
   самым сохраняя изначальный принцип «MapLibre + OSM без API-ключей» из
   раздела 2 этого файла. `RasterLayer`/`rememberRasterSource`/`OSM_TILE_URLS`
   убраны из `LiveTrackMap.kt` и `AggregatedFindsMap.kt` — свои слои (трек,
   маркеры, heatmap) остались как оверлей поверх векторной подложки. Проверено
   вживую на эмуляторе — подписи (`Amphitheatre Parkway` и т.п.) идеально
   чёткие даже при сильном приближении.

**Исправлено: карта текущей прогулки открывалась в точке по умолчанию
(0,0), если открыть её до нажатия «Start».** Причина в обеих платформенных
реализациях `LocationTracker` — они подписывались только на *будущие*
обновления GPS (`LocationManager.requestLocationUpdates` на Android,
`CLLocationManager.startUpdatingLocation()`/делегат на iOS), не отдавая сразу
уже закэшированное системой последнее известное местоположение. Пока не
случалось скачков GPS-провайдера, `currentLocation` в `RecordUiState`
оставался `null`, и `LiveTrackMap` показывал дефолтную точку. Это не зависело
от старта прогулки как такового — `locationTracker.track()` в
`RecordViewModel.init` слушает GPS независимо от `isRecording`, поэтому баг
проявлялся именно в задержке первого фикса. Исправлено эмитом кэшированной
точки сразу при подписке: Android —
`locationManager.getLastKnownLocation(provider)`, iOS — `manager.location`
(кэшированное свойство `CLLocationManager`, доступно без ожидания callback'а
делегата). Проверено вживую на эмуляторе с зафиксированной mock-геолокацией
(`adb emu geo fix`): открыл карту записи сразу после запуска приложения,
не нажимая «Start» — карта сразу показывает реальную точку, а не (0,0).

**Найден и исправлен ещё один краш навигации — воспроизводился и на Android,
и на iOS.** Симптом: если открыть карту текущей записываемой прогулки (кнопка
«View map» на экране «Запись», неважно, идёт запись или на паузе) и затем
нажать любую из нижних вкладок (Запись/Архив/Карта) — приложение падало.
Причина: `LeshyNavHost.kt` в `composable<Destination.RecordMap>` вызывает
`navController.getBackStackEntry(Destination.Record)` при каждой
рекомпозиции — чтобы взять тот же `viewModelStoreOwner`, что и у экрана
`Record` (см. Часть 2). Переход по нижней вкладке использует
`navigateToTopLevel` → `popUpTo(graph.findStartDestination().id)
{ inclusive = true; saveState = true }` (см. баг-фикс навигации в Части 5) —
это выталкивает запись `Record` из бэкстека СИНХРОННО, до того как успевает
закончиться recomposition уходящего экрана `RecordMap` во время
exit-анимации перехода. На этом кадре `getBackStackEntry(Destination.Record)`
бросает `IllegalArgumentException: No destination with route ... Record is
on the NavController's back stack` — краш. Тот же паттерн (без учёта того же
случая) был и в `composable<Destination.WalkMap>`
(`getBackStackEntry(Destination.WalkDetail(...))`) — тоже исправлен
превентивно, хотя пользователь этот путь ещё не проверял. Исправлено оборачиванием
обоих вызовов в `runCatching { ... }.getOrNull()` — если родительская запись
уже пропала (переход в процессе), composable просто ничего не рендерит на
этот короткий кадр вместо краша (экран всё равно анимированно исчезает).
Проверено вживую на эмуляторе длинной цепочкой переходов
(Запись→КартаЗаписи→Настройки→Запись→Архив→Детали→КартаПрогулки→Запись) —
крашей нет; экран `Record`/`RecordMap` корректно восстанавливается через
`restoreState=true` (последнее посещённое состояние внутри вкладки).

**~~Известное ограничение (НЕ исправлено...): `HeatmapLayer` крашит только
на эмуляторе, не на реальных устройствах~~ — ОПРОВЕРГНУТО и ИСПРАВЛЕНО
позже.** Первоначальное наблюдение (см. история ниже, оставлена как есть):
симптом при открытии экрана «Карта» — `Fatal signal 11 (SIGSEGV)` в
`RenderThread`, один и тот же адрес сбоя внутри `libmaplibre.so`, независимо
от данных и GPU-режима эмулятора; на тот момент казалось, что на реальном
физическом Pixel 4a тот же экран открывается без ошибок, поэтому краш
списали на графический стек эмулятора (ANGLE/SwiftShader) и решили не
чинить. **Это оказалось неверно.** Спустя несколько сессий пользователь
словил тот же краш на том самом Pixel 4a — приложение падало при заходе на
«Карту» после полного перезапуска. Разобрано через `adb logcat -b crash`
(понадобилось сначала включить «Отладка по USB» в Developer Options — была
выключена, из-за чего `adb devices` сперва ничего не видел): та же
сигнатура, `SIGSEGV` в `mbgl::android::MapRenderer::render` на
`RenderThread`, воспроизвелась 6 раз подряд за 25 минут реального
использования. Решающая улика — байты в регистрах `x3`/`x5`/`x7` (`x3 =
0x2d686561746d6170` и т.д.) побайтово декодируются в ASCII-строку
`"-heatmap"` — то есть нативный код разыменовывает как указатель память,
которая на самом деле хранит строку `id` heatmap-слоя (`"finds-heatmap"`) —
классическая type confusion где-то в JNI-мосте `maplibre-compose`/
`libmaplibre.so` конкретно для `HeatmapLayer`. Плюс отдельно обнаружено: изза
`restoreState=true` в навигации (см. фикс из Части 5) при крахе на вкладке
«Карта» каждый следующий запуск приложения тут же возвращал именно на неё —
краш-луп (5–17 секунд между падениями). **Исправление:** `HeatmapLayer` в
`AggregatedFindsMap.kt` заменён на кластеризованный `CircleLayer` —
`GeoJsonSource` с `GeoJsonOptions(cluster = true)` (встроенная
supercluster-кластеризация MapLibre, `point_count` как агрегированное
свойство на кластерных точках) + `step()`-выражение по `point_count`
(с фоллбэком `asNumber(const(1f))` для одиночных некластеризованных точек)
на `color`/`radius` — три уровня плотности (мало/средне/много →
синий/зелёный/красный круг), тот же визуальный принцип, что был у heatmap,
но `CircleLayer` — тот же тип слоя, что уже давно и без единого краша
рисует маркеры находок и точку геолокации в `LiveTrackMap`. Проверено вживую
на реальном Pixel 4a (после включения USB-отладки): записана тестовая
прогулка с 12 находками в одной точке, экран «Карта» открылся и отрисовал
сплошной красный круг без крашей; затем `am force-stop`+перезапуск
приложения 3 раза подряд с ручным переходом на «Карту» каждый раз — ни
одного падения (`adb logcat -b crash` — пусто), включая проверку на полном
реальном датасете пользователя (~70+ находок по нескольким прогулкам).
Тестовые прогулки удалены после проверки через уже существующую функцию
удаления прогулки из «Архива» — реальные данные пользователя не затронуты.

**Часть 6. Расширение каталога грибов (30 видов) + редизайн плашек.** Каталог
грибов вырос с 4 до 30 видов (из предоставленных пользователем
иллюстраций/данных), плашки на экране «Запись» переведены с цветной заливки
на фотографию гриба.

Решения:
- **Источник данных.** Присланные `mushrooms/*.png` (30 иллюстраций, каждая
  ~3.2 МБ, 1536×1024) и JSON-каталог с RU/EN-названиями, эталонным цветом и
  съедобностью. Второй JSON (`mushroom_marker_colors_by_filename.json`) был
  побайтово подмножеством первого — удалён. Каталог перенесён в
  `docs/mushrooms_catalog.json` (справочно, в рантайме не используется — см.
  ниже почему). Папка `mushrooms/` в корне удалена целиком.
- **Изображения пережаты и очищены от псевдо-прозрачности.** Оригиналы
  (1536×1024, ~83 МБ на все 30) пережаты через `sips -Z 480` до 480px по
  длинной стороне (~5.6 МБ суммарно) и положены в
  `shared/src/commonMain/composeResources/drawable/` (Compose Resources не
  поддерживает подпапки для организации — только qualifier-суффиксы, поэтому
  структура плоская). Часть исходников (те, что были экспортированы как
  `RGB` без альфа-канала) содержали не настоящую прозрачность, а слабый
  чёрно-белый шахматный узор, растрированный прямо в пиксели (визуально
  почти незаметен, но при рендере в плашке с реальным белым/светлым фоном
  давал заметный «шум» вместо чистого фона). Исправлено Python/Pillow-скриптом
  (не сохранён в репозитории, разовый прогон): flood fill почти-белых
  (`r,g,b > 232`, разброс каналов `< 12`) пикселей от границ изображения —
  найденная область помечается прозрачной (`alpha = 0`). Flood fill (а не
  плоский порог по всему изображению) специально выбран, чтобы не задеть
  светлые/белые части самих грибов (шампиньон, дождевик и т.п.) — они не
  связаны с фоном непрерывной областью почти-белых пикселей. Изображения,
  экспортированные сразу с настоящим альфа-каналом, скрипт не тронул
  (0 изменённых пикселей).
- **`iconRef` теперь используется** (в Части 2–5 объявлено, но нигде не
  читалось) — хранит имя файла без расширения (`"boletus_edulis"`). На
  UI-стороне — `Res.allDrawableResources["boletus_edulis"]` (генерируемая
  Compose Resources `internal val Map<String, DrawableResource>`, доступна
  из любого файла модуля `shared` благодаря Kotlin `internal`), а не
  статический `Res.drawable.boletus_edulis` — статические accessor'ы
  нельзя выбирать по строке из БД в рантайме.
- **Новое поле `Category.edibilityStatus` (`EdibilityStatus`
  enum — `EDIBLE`/`CONDITIONALLY_EDIBLE`/`INEDIBLE`)**, а не хранение сырого
  6-значного `edibility.status` из JSON (`edible`/`conditionally_edible`/
  `mixed`/`inedible`/`poisonous`/`deadly_poisonous`) — в UI нужны только 3
  цвета значка (зелёный/жёлтый/красный), сведение JSON-статусов к 3 корзинам
  сделано один раз при переносе данных в Kotlin, хранить 6 вариантов ради
  неиспользуемой детализации избыточно.
- **Миграция БД v1→v2 — не destructive.** Добавляет колонку
  `edibilityStatus` (`ALTER TABLE ... ADD COLUMN ... DEFAULT 'EDIBLE'`) и
  **переименовывает на месте** 4 старые дефолтные категории
  (`category_porcini`/`category_chanterelle`/`category_ryzhik`/
  `category_boletus`) в их эквиваленты из нового 30-видового каталога
  (`category_boletus_edulis`/`category_cantharellus_cibarius`/
  `category_lactarius_deliciosus`/`category_leccinum_scabrum`) — `UPDATE`,
  не `DELETE`+реинсерт. Причина: `DELETE` каскадно снёс бы у пользователя
  уже отмеченные находки (`objects.categoryId` → `ON DELETE CASCADE`) на
  любом устройстве, где приложение уже стояло, а строка с тем же `id`
  продолжает быть валидной FK-целью и после переименования. Значения
  `isActive`, выставленные пользователем в «Настройках» до миграции,
  тоже переживают апгрейд как есть (проверено на эмуляторе `Medium_Phone`
  с деинсталляцией из более ранней части — «Рыжик» был выключен ещё в
  прошлой сессии и остался выключенным после миграции).
- **`EnsureDefaultCategoriesUseCase` меняет стратегию заполнения**: раньше
  сеялось всё сразу и только если таблица была абсолютно пустой
  (`count() == 0`), теперь — по каждой категории каталога отдельно,
  insert только если такого `nameKey` ещё нет (`getByNameKey(...) == null`).
  Так апгрейд существующей установки (уже 5 строк после миграции) добирает
  оставшиеся 26 новых видов, а не пропускает заполнение целиком из-за
  непустой таблицы.
- **Плашка (`MushroomTile.kt`) переработана.** Верхняя часть: `Image`
  гриба на весь блок (`ContentScale.Fit`, чтобы не обрезать иллюстрацию —
  фон-подложка Card видна по краям, где не хватает изображения, что и даёт
  требуемый «цвет по умолчанию» позади фото), значок съедобности (цветной
  кружок с белой обводкой) в правом верхнем углу поверх изображения,
  название гриба прибито к низу верхней части. Название рисуется вручную
  через `Canvas` + `TextMeasurer.measure()` + `drawText(...)` три раза
  одним и тем же измеренным layout'ом (без пере-измерения) с разным
  `drawStyle`: чёрная толстая обводка (`Stroke(8f)`) → тонкая обводка
  цветом категории (`Stroke(4f)`) → белая заливка сверху — «двойная
  окантовка», per задание, к тому же увязывает подпись с цветом
  контура карточки. `Card` получил `border = BorderStroke(2.dp,
  category.colorHex)` — раньше контура не было вообще. Общий
  `colorHex`→`Color` парсинг вынесен в `ui/util/ColorUtils.kt`
  (`parseHexColor`) — раньше был продублирован inline в одном месте, теперь
  переиспользуется. Нижняя часть плашки (−/счётчик/+) не тронута, как и
  просили.
- **`AppLanguage`/`categoryDisplayName`/`Strings.kt` без структурных
  изменений** — просто выросли на 26 новых `StringKey` (30 новых минус 4
  старых, которые убраны как замещённые). RU/EN-названия — первое имя из
  `common_names_ru[0]`/`common_names_en[0]` в JSON, с заглавной буквы.
- Проверено вживую на эмуляторе `Medium_Phone` (свежая пересборка APK,
  `adb install -r` + `force-stop`+`start` для гарантии перезапуска процесса,
  не переиспользования уже запущенного): все 30 плашек рендерят фото,
  цветной контур, зелёный/жёлтый/красный значок съедобности строго по
  ожидаемым корзинам, двухстрочные длинные названия («Трубчатая лисичка»,
  «Груздь настоящий») переносятся и остаются читаемыми. Экран «Настройки»
  со списком из 30 переключателей открывается и скроллится без крашей.
  `./gradlew :shared:compileAndroidMain`, `:shared:compileKotlinIosArm64`,
  `:shared:compileKotlinIosSimulatorArm64` и `:androidApp:assembleDebug`
  чистые; Room экспортировал `shared/schemas/.../2.json`. iOS живьём в этой
  сессии не запускался (нет активного Xcode-запуска в рамках этой правки) —
  общий Compose-код тот же, что уже проверялся вживую на iOS в Части 4/5,
  разница только в данных категорий и разметке плашки.

**Баг найден и исправлен на реальном Android-телефоне (не воспроизводился на
эмуляторе так же явно): двойная окантовка подписи плашки рендерилась
некорректно.** На реальном устройстве белая середина обводки была неоправданно
широкой (чёрный/белый/чёрный слои перепутывались местами), на iOS-симуляторе
внутренний чёрный слой не проявлялся вовсе. По итогам разбора и по прямому
пожеланию пользователя дизайн подписи упрощён с «двойной» (чёрная толстая +
цветная тонкая обводка + белая заливка) до одинарной (чёрная обводка + белая
заливка) — пользователь заметил, что именно такой вариант (уже виден на iOS
из-за бага выше) читается лучше всего на обоих платформах.
- **Корневая причина рендер-бага — не в самой идее двойной обводки, а в
  способе рисования.** Изначальная реализация мерила текст один раз через
  `TextMeasurer.measure()` и затем звала `drawText(layout, color = ...,
  drawStyle = ...)` несколько раз с разными `color`/`drawStyle` поверх ОДНОГО
  и того же `TextLayoutResult`. Экспериментально подтверждено (тест с
  огромной ярко-красной обводкой 20.dp/80.dp) — второй вызов `drawText`
  визуально портил результат первого (вместо красной обводки — блобообразная
  белая клякса без чёткой формы букв), то есть вызовы `drawText` с одним и тем
  же `TextLayoutResult`, но разными `color`/`drawStyle`, не независимы друг от
  друга — похоже на переиспользуемый/мутируемый Paint или кэш `TextMeasurer`
  между вызовами (`TextMeasurer` явно документирует внутренний кэш по
  layout-параметрам, но не гарантирует независимость по paint-параметрам вроде
  `color`/`drawStyle`). Попытка измерить текст ДВАЖДЫ (два независимых
  `TextLayoutResult`, каждый под свой стиль) через один и тот же
  `TextMeasurer` не помогла — тот же артефакт, значит кэш общий даже между
  раздельными вызовами `measure()`.
- **Итоговое решение — два наложенных обычных `Text()`** (не ручной
  `Canvas`+`TextMeasurer`) в `Box(contentAlignment = Alignment.BottomCenter)`:
  один с `drawStyle = Stroke(width = ...)` и чёрным цветом (обводка), второй
  поверх — обычная белая заливка. Каждый `Text()` владеет своим независимым
  layout-состоянием (`remember`, скоуп конкретного call site в композиции), а
  не общим кэшем — тем самым исходный баг не воспроизводится. Ширина обводки
  переведена в `with(LocalDensity.current) { 3.dp.toPx() }` вместо сырого
  `Float` (изначальная реализация ошибочно использовала «8f»/«4f» как будто
  raw px, без density-множителя — тоже вносило непредсказуемость толщины
  между устройствами с разной плотностью экрана, независимо от бага выше).
- Проверено вживую на реальной последовательности: пересобранный APK
  переустанавливался (`adb install -r` + `force-stop`/`start`) на эмуляторе
  `Medium_Phone` несколько раз подряд по мере итераций (голая толщина 2.5dp
  → 4dp чёрным, затем диагностический огромный красный 12dp/80px, затем
  финальная версия) — на каждом шаге сверялся самый тёмный пиксель подписи
  через Pillow (`(0,0,0)` только в финальной версии, до этого — не темнее
  `(209,206,210)`, то есть визуально «похоже на чёрный контур» на скриншоте в
  уменьшенном/интерполированном виде было артефактом даунскейла, не реальным
  чёрным в исходных пикселях PNG). Итоговый вариант проверен и на EN, и на RU
  строках (однострочные и двухстрочные названия) — обводка чистая и ровная в
  обоих случаях.

**Отдельный запрос пользователя выполнен следом: порядок плашек грибов
на экране «Запись».** Раньше `order` в `DEFAULT_CATEGORIES` шёл в порядке
исходного JSON-каталога (без особой логики). Теперь — съедобные, затем
условно съедобные, затем несъедобные (по `edibilityStatus`), и внутри каждой
группы по алфавиту русского отображаемого имени (`common_names_ru[0]`).
- **`EnsureDefaultCategoriesUseCase.invoke()` научился «доводить до кондиции»
  уже существующие строки**, а не только доливать отсутствующие: для каждой
  категории каталога, если строка в БД уже есть, но отличается от канонических
  данных (`order`/`colorHex`/`iconRef`/`edibilityStatus`) — она обновляется
  апдейтом по существующему `id`, `isActive` при этом сохраняется как есть
  (это единственное пользовательское поле, остальное — источник истины
  сам каталог). Без этого правки вроде переупорядочивания просто не
  применялись бы на уже отсеянных тестовых установках (эмулятор, реальный
  телефон) — понадобилась бы ещё одна `Migration`, хотя `order` — чисто
  визуальное поле и это явный overkill. Проверено вживую: на том же
  `Medium_Phone`, где категории были посеяны ДО этого изменения (со старым
  порядком), после переустановки APK (без сброса данных) плашки сразу
  показались в новом порядке (Белый гриб → Вешенка → Гриб-зонтик → Козляк —
  первые 4 съедобных по алфавиту), включая случай с уже где-то выключенным
  вручную «Рыжик» (`isActive=false` не сбросился синхронизацией).

**Найден и исправлен баг edge-to-edge отступов: по всем четырём краям экрана
(и на Android, и на iOS) оставалась светло-серая/лавандовая рамка из
неиспользуемых белых полей вместо темы приложения.** Причина —
`App.kt`: корневой `Scaffold` был обёрнут в `Modifier.safeContentPadding()`.
Этот модификатор объединяет `safeDrawing` (реальные аппаратные отступы —
статус-бар, вырез камеры, клавиатура) **и** `safeGestures` (искусственные
зоны избегания жестов ОС — edge-swipe-back и т.п.), причём применяет их к
самому фону `Scaffold`, а не только к контенту. Поскольку в приложении нет
собственных edge-swipe-жестов (только тапы), `safeGestures`-часть отступа
была не нужна — но эффект от `safeContentPadding()` был хуже: он инсетил
сам `Surface`/фон `Scaffold` целиком, из-за чего тема (бледно-лавандовый
`background`/`surface`, унаследованный от `lightColorScheme(primary=...,
secondary=...)`) не доходила до системных панелей, и под статус-баром/
жестовой полосой был виден чистый белый цвет по умолчанию.
- **Исправление:** `Modifier.safeContentPadding()` убран с самого `Scaffold`
  целиком (стандартный Material3 edge-to-edge паттерн) — `TopAppBar` и
  `NavigationBar` сами инсетят СВОЙ контент через `WindowInsets.statusBars`/
  `WindowInsets.navigationBars` по умолчанию, а их цвет фона (унаследованный
  от темы) при этом продолжается под системными панелями до самого края
  экрана. Проверено по исходникам Material3 1.11.0-alpha07
  (`ScaffoldDefaults.contentWindowInsets = WindowInsets.systemBarsForVisualComponents`,
  и `Scaffold` учитывает top/bottom-инсеты в `innerPadding` только если
  `topBar`/`bottomBar` отсутствуют — у нас оба есть, значит `innerPadding`,
  прокидываемый в `LeshyNavHost(contentPadding = innerPadding)`, не ломается
  удалением внешнего модификатора).
- Заодно у `LazyVerticalGrid` на экране «Запись» (`RecordScreen.kt`) добавлен
  горизонтальный `contentPadding` (8.dp, раньше был только вертикальный) —
  чисто эстетический отступ, чтобы кнопки +/− крайних плашек не были
  вплотную к истинному краю экрана теперь, когда сам `Scaffold` больше не
  добавляет инсет слева/справа.
- Проверено вживую на эмуляторе `Medium_Phone` (пересборка + `adb install -r`
  + `force-stop`/`monkey`-запуск): экраны «Запись» и «Карта» — статус-бар и
  нижняя панель показывают тему приложения (лавандовый фон) вплоть до самого
  края, часы/иконки статус-бара и жестовая полоска остаются читаемыми поверх
  него; белых полей по краям не осталось. На iOS в этой сессии живьём не
  проверялось (доступа к Mac/Xcode не было) — общий Compose-код тот же,
  разница платформ только в том, как хост-система рисует системные панели
  поверх `WindowInsets`, что реализовано в самом Compose Multiplatform, а не
  в коде приложения.
- Закоммичено отдельным коммитом от расширения каталога грибов.

**Исправлена калибровка тепловой карты находок (`AggregatedFindsMap.kt`):
любая одиночная находка красилась в чистый красный, как будто это уже
«хотспот».** Причина — `HeatmapLayer` со значениями по умолчанию
(`intensity = 1.0`, `weight = 1.0`): плотность в самой яркой точке одиночной
находки уже достигает пика цветовой шкалы (0→прозрачный, 0.1→синий,
0.3→циан, 0.5→лайм, 0.7→жёлтый, 1→красный), так что «красный» ничем не
отличался от «здесь вообще что-то нашли». Правки в сессию до этой (не
закоммиченные, начатые и брошенные на середине) уже нашли верное
направление — понижать `intensity` — но были отменены обратно к последнему
коммиту (`git checkout HEAD --`) и переделаны заново с нуля, начисто, из-за
неуверенности, была ли последняя выбранная константа вообще проверена
живьём до прерывания сессии.
- **Решение — тот же рычаг (`intensity`), пересчитанный заново.** Плотность
  находок в `HeatmapLayer` накапливается аддитивно для близких/перекрывающихся
  точек (по документации самого `weight` — точка веса 10 эквивалентна 10
  наложенным точкам веса 1), поэтому равномерное понижение `intensity`
  растягивает эту плотность по всей шкале 0..1 вместо мгновенного насыщения
  на первой же находке. Целевой ориентир (не выведен формулой, а подобран
  как разумная отправная точка): одна находка — на нижней границе синего,
  ~5 находок в одном месте — зелёный/лайм, ~10+ — жёлтый/красный.
  `HEATMAP_INTENSITY = 0.02f`, `HEATMAP_RADIUS = 20.dp` (было 30.dp по
  умолчанию — чуть уже, чтобы «одно и то же место» не растягивалось слишком
  широко) в `ui/map/AggregatedFindsMap.kt`. Цветовая шкала (`color`) не
  трогалась — дефолтная `LayerDefaults.HeatmapColors` в самой библиотеке уже
  делает ровно то, что нужно (синий→циан→лайм→жёлтый→красный по плотности),
  проблема была только в масштабе входного значения, а не в самой шкале.
- **Сознательно не стали делать relative-масштабирование** (нормировать
  плотность на максимум по видимым данным) — если у пользователя вообще нет
  кластеров (все находки по одной), такая нормализация всё равно выкрасила бы
  «самую одинокую из одиноких» находок в красный, что вводит в заблуждение
  ровно так же, как исходный баг. Фиксированная (абсолютная) интенсивность
  честнее: красный обозначает «здесь реально много находок», а не «здесь
  больше находок, чем в среднем по остальной карте».
- **Точное значение не выведено математически, а перепроверено логически**
  по единственной имеющейся зацепке из прерванной сессии (комментарий,
  утверждавший, что `intensity = 0.18` всё ещё красил одиночную находку в
  полный красный) — подгонка `weight`/`intensity` линейна, поэтому по этой
  точке можно прикинуть отправной коэффициент; независимо выведенное
  значение сошлось с уже выбранными в прерванной сессии `0.02`, так что
  константа перенесена как есть, а не заменена на новую догадку.
- **Проверено вживую на iOS-симуляторе (iPhone 17, iOS 26.5)** —
  `HeatmapLayer` крашит рендерер только Android-эмулятора на этой машине (см.
  выше, «Известное ограничение»), iOS-симулятор для heatmap не затронут этим
  багом. Собрано через `xcodebuild` + `xcrun simctl install`/`launch`, тестовые
  находки посеяны напрямую в `Documents/leshy.db` (через `sqlite3`, приложение
  предварительно остановлено): три группы на одной прогулке — 1 одиночная
  находка, 5 находок в одной точке, 12 находок в одной точке, разнесённые
  друг от друга на ~200–400 м. Заодно вычищены две старые прогулки-заглушки с
  координатами `(0,0)` (мусор из более ранней сессии тестирования), иначе
  они разбивали bounding box на весь земной шар и мешали приблизиться к
  тестовым точкам. Результат на экране «Карта»: одиночная находка —
  чистый синий без примеси другого цвета, 5 находок — зелёно-лаймовое ядро,
  12 находок — красное ядро с зелёно-синим спадом к краям, то есть ровно
  то трёхуровневое разделение (мало→синий, средне→зелёный, много→красный),
  которое просили. Тестовые данные удалены из БД симулятора после проверки
  (`walks`/`objects`/`track_points` очищены, симулятор оставлен пустым).
  Компиляция также чистая: `./gradlew :shared:compileAndroidMain`,
  `:shared:compileKotlinIosArm64`, `:shared:compileKotlinIosSimulatorArm64`,
  `:androidApp:assembleDebug`.

**Добавлено удаление прогулки из «Архива».** Раньше ошибочно созданную
прогулку нельзя было ничем исправить — не было ни одного пути стереть её из
БД. На детальном экране прогулки (`WalkDetailScreen`) верхняя панель
получила третий элемент: слева стрелка назад, по центру название прогулки,
справа — иконка мусорной корзины (`Icons.Filled.Delete`, `actions` у
`TopAppBar`, раньше не заполнялся). По тапу — `AlertDialog` (первый в
проекте — до этого `AlertDialog`/`Dialog` нигде не использовался) с
текстом-предупреждением о необратимости и кнопками Да/Нет; «Нет» просто
закрывает диалог, «Да» удаляет.
- **Данные удалять было не нужно почти нигде дописывать** — `WalkRepository.delete(walk)`
  и `WalkDao.delete` (`@Delete`) существовали с Части 1, просто не были
  подключены ни к одному экрану. `objects`/`track_points` объявлены с
  `onDelete = CASCADE` на `walks` (см. раздел 4) — единственный вызов
  `walkDao.delete(walkEntity)` сам чистит находки и точки трека этой
  прогулки, поэтому они гарантированно пропадают и с тепловой карты.
- **Состояние диалога и факта удаления — в `WalkDetailUiState`**, не в
  локальном `remember` композable, по правилу проекта «Immutable UiState +
  StateFlow на каждый экран» (раздел 5, п.5). `WalkDetailViewModel` держит
  два своих `MutableStateFlow` (`showDeleteConfirmation`, `deleted`) отдельно
  от Room-based `combine()` четырёх репозиториев-флоу (`walk`/`marks`/
  `trackPoints`/`categories`) и объединяет их вторым `combine()` в финальный
  `WalkDetailUiState` — тот же паттерн разделения «данные из БД» / «чисто UI
  state», что уже описан для `MapViewModel` в Части 4. Без этого разделения
  собственный `combine()` от Room перезаписывал бы флаги диалога при каждой
  эмиссии (например, после самого удаления `observeById` эмитит `null` и
  сбросил бы `deleted` обратно на `false`).
- **`deleted: Boolean`** — одноразовый сигнал «прогулка удалена», на который
  `WalkDetailScreen` реагирует через `LaunchedEffect(uiState.deleted)`,
  вызывая уже существующий `onBack()` (обычный `navController.popBackStack()`
  из `LeshyNavHost`) — отдельный callback/маршрут не понадобился.
- Добавлено 5 новых `StringKey` (`WalkDetailDeleteContentDescription`,
  `WalkDetailDeleteConfirmTitle/Message/Yes/No`) с RU/EN-переводами в
  `Strings.kt`, по уже существующему паттерну (раздел 5, `Strings.kt`
  exhaustive `when`).
- Проверено вживую на эмуляторе `Medium_Phone`: свежая пересборка +
  переустановка APK, записаны две тестовые прогулки (одна с 1 находкой
  «Bovine bolete», другая пустая). На прогулке с находкой — открыт диалог,
  нажатие «Нет» закрывает диалог без изменений (прогулка и находка остались
  на месте), повторное открытие диалога и нажатие «Да» удаляет прогулку —
  экран автоматически возвращается в «Архив», прогулка с находкой пропадает
  из списка (находка соответственно не может остаться и на тепловой карте,
  раз строка `objects` удалена каскадно). Обе тестовые прогулки удалены после
  проверки — архив снова пуст, как и до тестирования. Компиляция чистая:
  `./gradlew :shared:compileAndroidMain`, `:shared:compileKotlinIosSimulatorArm64`,
  `:androidApp:assembleDebug`.

**Исправлено: запись трека останавливалась, если во время прогулки выключить
экран телефона обычным способом (не force-quit).** Причина — GPS-подписка жила
только в `RecordViewModel.viewModelScope` без какого-либо механизма, который
удерживал бы приложение «на переднем плане» с точки зрения ОС: на Android без
активного foreground-сервиса с типом `location` система через некоторое время
после ухода с экрана (блокировка, другое приложение поверх) глушит/останавливает
колбэки `LocationManager.requestLocationUpdates` — ровно то поведение, которое
описал пользователь. На iOS без записи `location` в `UIBackgroundModes` и
`CLLocationManager.allowsBackgroundLocationUpdates` система аналогично не
доставляет обновления геолокации в фоне.
- **Явный выбор пользователя по scope фикса** (задан вопросом при разборе задачи):
  чинится именно блокировка экрана/уход в фон, а не полный force-quit/свайп
  приложения из Recents — второе потребовало бы отдельно персистить активный
  `walkId` вне памяти процесса и перезапускать сервис/сессию при рестарте,
  осознанно не делалось.
- **Android — новый `WalkRecordingService`** (`shared/androidMain/data/platform/WalkRecordingService.kt`,
  рядом с `AndroidLocationTracker` по уже устоявшемуся паттерну platform-actual
  в `shared`, а не в `androidApp`). Сервис **не дублирует GPS-подписку** — вся
  логика получения точек и записи их в БД как и раньше живёт в
  `RecordViewModel`/`RecordTrackPointUseCase` (единый источник истины, без
  риска задвоить точки трека). Единственная задача сервиса — быть запущенным
  foreground-сервисом с `foregroundServiceType="location"` всё время, пока
  идёт запись прогулки: по документации Android это переводит **всё
  приложение** (не только сам сервис) в исключение из фоновых лимитов на
  доставку геолокации, поэтому существующая подписка в `viewModelScope`
  просто продолжает получать обновления как ни в чём не бывало. Обязательный
  постоянный notification («Идёт запись прогулки» / «Recording your walk»,
  канал `walk_recording`, иконка — системная `ic_menu_mylocation`, без
  отдельного ассета) — по тапу открывает приложение, без отдельной кнопки
  остановки (за пределами выбранного scope). Язык для текста уведомления
  передаётся через `Intent extra` в момент `start()` (значение `currentLanguage`
  уже есть в `RecordViewModel` на этот момент) — специально не читается внутри
  сервиса асинхронно через `SettingsRepository`/DataStore, чтобы
  `startForeground()` вызывался синхронно сразу в `onStartCommand` без гонки
  (Android требует вызвать его быстро после старта сервиса).
- **`BackgroundRecordingController`** — новый `expect`-подобный интерфейс
  (обычный `interface` в `commonMain/data/platform/`, т.к. реализации не нужен
  `expect class`, только Koin-биндинг per платформа) с `start(language)`/`stop()`.
  Android-реализация запускает/останавливает `WalkRecordingService`
  (`ContextCompat.startForegroundService`/`stopService`). iOS-реализация —
  no-op: там та же роль закрыта не отдельным компонентом, а двумя флагами
  прямо на `CLLocationManager` в `IosLocationTracker`
  (`allowsBackgroundLocationUpdates = true`, `pausesLocationUpdatesAutomatically
  = false`) + записью `UIBackgroundModes: [location]` в `Info.plist` — этого
  достаточно, чтобы CoreLocation продолжал доставлять обновления уже
  подписанному делегату, пока процесс жив, без запроса повышенного разрешения
  «Always» (тот же приём, что у фитнес-трекеров: активная сессия, начатая на
  переднем плане с обычным «When In Use», продолжается в фоне).
  `RecordViewModel` получил новый конструкторный параметр
  (`presentationModule`/`PresentationModule.kt` — добавлен ещё один
  позиционный `get()`), вызывает `start(currentLanguage)` в `start()` и
  `stop()` в `finish()`.
- **AndroidManifest.xml**: добавлены разрешения `FOREGROUND_SERVICE`,
  `FOREGROUND_SERVICE_LOCATION` (обязательно для типа `location` с
  targetSdk 34+) и `POST_NOTIFICATIONS`, плюс сама `<service>` с
  `android:foregroundServiceType="location"`. `MainActivity.onCreate` теперь
  запрашивает `POST_NOTIFICATIONS` в общем батче рантайм-разрешений вместе с
  геолокацией/камерой, но только на `Build.VERSION.SDK_INT >=
  TIRAMISU` — без него уведомление на Android 13+ молча не показывается, но
  сам сервис и фоновый трекинг всё равно продолжают работать (разрешение
  влияет только на видимость уведомления, не на сам foreground-статус).
- Проверено вживую на эмуляторе (`Medium_Phone`, свежая сборка + `adb install
  -r`): записана тестовая прогулка с мок-геолокацией (`adb emu geo fix`),
  подтверждено `dumpsys activity services` — `isForeground=true
  foregroundId=1 types=0x00000008` (0x8 = `FOREGROUND_SERVICE_TYPE_LOCATION`),
  `dumpsys notification` показывает запощенное уведомление с ожидаемым
  текстом. Экран выключен командой `input keyevent 26` (`mWakefulness`
  подтверждён как `Asleep` через `dumpsys power`, `mResumedActivity` пуст —
  Activity действительно не на переднем плане), затем 5 новых мок-фиксов GPS
  поданы с интервалом 5 секунд. БД (вытащена вместе с `-wal`/`-shm`, т.к.
  Room пишет в WAL-режиме и файл без них показывает 0 строк) подтвердила
  5 новых `track_points`, записанных именно во время «сна» экрана — итог 8
  точек и 2.22 км при завершении прогулки, таймер тоже продолжал тикать
  (`viewModelScope` пережил уход Activity в фон, как и ожидалось). После
  `Finish` — `dumpsys activity services`/`dumpsys notification` подтвердили,
  что сервис и уведомление корректно остановились. Тестовая прогулка удалена
  через уже существующую функцию удаления из «Архива».

**iOS живьём проверен отдельно, в следующей сессии (доступ к Mac/Xcode
появился).** Собрано через `xcodebuild` + установлено/запущено на симуляторе
(iPhone 17, iOS 26.5). Главный риск от этой правки — `manager
.allowsBackgroundLocationUpdates = true` кидает `NSInvalidArgumentException`
(немедленный краш), если `UIBackgroundModes` в Info.plist не настроен — этот
риск снят: экран «Запись» открывается без крашей, `RecordViewModel.init`
(который синхронно подписывается на `locationTracker.track()` и выставляет
этот флаг) отрабатывает нормально. Статус-бар показывает системную стрелку
активной геолокации в фоне (тот же индикатор, что и у Maps/Strava) —
подтверждает, что `allowsBackgroundLocationUpdates`/`UIBackgroundModes`
реально включили фоновый режим на уровне ОС.
- **Полный сквозной тест (фон + новые GPS-точки → запись в БД) сделать не
  удалось — упёрлись не в баг кода, а в ограничение самого iOS-симулятора.**
  По логам CoreLocation (`log show --predicate 'process == "leshy"'`):
  `startUpdatingLocation` вызывается ровно один раз при подписке, тут же
  приходит ОДИН `didUpdateLocations` (закэшированный фикс), после чего —
  ни одного нового колбэка, сколько бы `xcrun simctl location set/start`
  ни подавалось (пробовали и одиночные `set`, и маршруты `start` с разными
  скоростями/интервалами, до 45 секунд ожидания). `authorizationStatus:4`
  (`authorizedWhenInUse`) в логе подтверждает, что дело не в разрешении.
  Это известное ограничение CLI-инструмента `simctl location` (в отличие от
  Android-эмулятора, где `adb emu geo fix` действительно продолжает
  генерировать свежие колбэки) — на практике для непрерывной симуляции
  маршрута Apple рассчитывает на GPX-файл, подключаемый через Xcode Scheme
  во время `Run` из самой Xcode, а не на постфактум `simctl` из командной
  строки к уже запущенному через `xcodebuild`/`simctl launch` процессу.
  Поэтому сквозной тест «свернуть → накопить track_points → развернуть,
  проверить БД» (как на Android) технически не воспроизводим в этой
  среде — не потому что код не работает, а потому что нет способа доставить
  вторую живую GPS-точку процессу после старта.
- Тестовые прогулки, случайно созданные в процессе (id из `walks`, все с
  `startTime` этой сессии, `endTime` пустой), удалены — из-за нестабильности
  UI-автоматизации на симуляторе (окно Simulator.app в какой-то момент
  перестало открываться через System Events/AppleScript, помогло только
  прямое SQL-удаление тестовых строк по `id`, не трогая более старую
  прогулку из предыдущей сессии).
- **Вывод: код корректен и не крашит (главный измеримый риск снят), но
  «живое» сквозное подтверждение непрерывной фоновой записи трека на iOS
  специфично для реального устройства** — на симуляторе `simctl location`
  не обеспечивает continuous delivery уже запущенному процессу. Если нужна
  такая проверка, придётся либо использовать GPX-маршрут через Xcode Scheme
  (`Edit Scheme → Run → Options → Core Location`), либо тестировать на
  реальном iPhone.
  `./gradlew :shared:compileAndroidMain :androidApp:assembleDebug
  :shared:compileKotlinIosArm64 :shared:compileKotlinIosSimulatorArm64` —
  чисто.

**Экран «Настройки»: шестерёнка заменена на кнопку «назад».** Раньше
верхний `TopAppBar` (общий на всё приложение, задан один раз в `App.kt`)
всегда показывал одну и ту же иконку-шестерёнку в `actions` — в том числе
уже находясь на самом экране «Настройки», где она была бесполезна (повторный
тап просто пере-навигировал на тот же экран). Теперь `actions` в `App.kt`
рендерит иконку условно: на «Настройках» — стрелка назад
(`Icons.AutoMirrored.Filled.ArrowBack`, `contentDescription = null`, тот же
паттерн decorative-иконки, что уже используется на `WalkDetailScreen`/
`RecordMapScreen`/`WalkMapScreen`, т.к. рядом всегда есть текстовый
заголовок), на всех остальных экранах — прежняя шестерёнка.
- **Возврат — на тот же таб нижней навигации, с которого зашли в
  «Настройки», а не всегда на «Запись».** Добавлен `remember`-стейт
  `lastMainDestination` в `App()`: каждый тап по `NavigationBarItem`
  (Запись/Архив/Карта) сохраняет туда свою `Destination` перед навигацией;
  тап по шестерёнке эту переменную не трогает. Кнопка «назад» на
  «Настройках» зовёт уже существующий `navigateToTopLevel(lastMainDestination)`
  — тот же самый pop/save/restore механизм, что и у обычных табов и самой
  шестерёнки (см. фикс навигации в Части 5), поэтому никакого нового способа
  навигации не появилось — просто симметричный вызов в другую сторону.
  «Предыдущий экран» здесь понимается на уровне таба (Запись/Архив/Карта),
  не глубже: если зайти в «Настройки» из вложенного экрана (например,
  `RecordMap` или `WalkDetail`), возврат восстанавливает таб на его
  последнем сохранённом состоянии через `restoreState=true`, а не именно тот
  вложенный экран — так же, как уже ведёт себя обычное переключение табов
  нижней навигации в этом приложении.
- Проверено вживую на эмуляторе `Medium_Phone`: переключился на таб
  «Карта» (с непустым фильтром периода), открыл «Настройки» через
  шестерёнку — иконка в `TopAppBar` сразу сменилась на стрелку назад — и
  нажал её: вернулся именно на «Карту» с тем же выбранным фильтром (не на
  «Запись» по умолчанию). Повторено с таба «Запись» во время активной
  записи прогулки (таймер тикал) — после возврата из «Настроек» запись
  продолжилась без сброса (тот же `RecordViewModel`, что и раньше). Тестовая
  прогулка, случайно начатая при промахе мимо кнопки во время тестирования,
  завершена и удалена через «Архив» — как и до тестирования, в архиве
  осталась только более ранняя прогулка. `./gradlew :shared:compileAndroidMain
  :shared:compileKotlinIosSimulatorArm64 :androidApp:assembleDebug` — чисто.

**Уточнение дизайна кнопки «назад» на «Настройках» (следом за фикстом выше).**
По прямому пожеланию пользователя кнопка стала текстовой (`TextButton`, а не
`IconButton`) — показывает и подпись («Назад»/«Back», новый `StringKey.
SettingsBackButtonLabel`), и иконку рядом (текст слева, иконка справа: `Text`
→ `Spacer(4.dp)` → `Icon`). Значок сменён с прямой `Icons.AutoMirrored.Filled.
ArrowBack` на `Icons.AutoMirrored.Filled.Undo` — крючкообразную стрелку
(похожа на классическую иконку «отменить», по образцу присланному
пользователем: дуга сверху вниз, заканчивающаяся остриём влево) — по мнению
пользователя, читается яснее как «назад» в паре с текстом, чем прямая
стрелка. Библиотека `material-icons-extended` уже была в зависимостях
(`shared/build.gradle.kts`), новый значок ничего не добавил в граф зависимостей.
`contentDescription` иконки — по-прежнему `null` (декоративная, значение уже
несёт видимый текст рядом, `TextButton` мержит их в один accessibility-узел).
Проверено вживую на эмуляторе `Medium_Phone`: кнопка «Back ↩ / Назад ↩»
отображается и работает на обоих языках без переносов/обрезки текста,
навигация на предыдущий таб — та же, что и в предыдущем фиксе (не менялась).
`./gradlew :shared:compileAndroidMain :shared:compileKotlinIosSimulatorArm64
:androidApp:assembleDebug` — чисто.

**`IPHONEOS_DEPLOYMENT_TARGET` понижен с 18.2 до 15.0 в `iosApp.xcodeproj`
(применено, не просто заметка).** 18.2 был дефолтом Xcode-визарда с момента
скаффолдинга проекта в Части 1, а не осознанным выбором под конкретный API.
Понадобилось для тестирования на реальном iPhone SE пользователя (iOS
15.8.8) — с 18.2 живое устройство ниже этой версии просто не могло запустить
сборку. Эмпирически проверено (методом подбора: пробовали 13.0 → ошибка
компиляции, 14.0 → сборка чистая, но линкер предупреждает про несоответствие
версий у `Shared.framework`, 15.0 → предупреждения уходят, сборка и живой
запуск на симуляторе чистые):
- MapLibre (`MapLibre.framework/Info.plist` → `MinimumOSVersion`) требует
  только iOS 12.0.
- `iosApp/iosApp/iOSApp.swift` (SwiftUI `App`/`Scene`/`WindowGroup`) требует
  iOS 14.0 — ниже это жёсткая ошибка компиляции Swift.
- **Реальный потолок — сам `Shared.framework`** (Kotlin/Native 2.4.0 +
  Compose Multiplatform 1.11.1 из текущих `libs.versions.toml`): при таргете
  ниже 15.0 линкер пишет `ld: warning: object file ... was built for newer
  'iOS-simulator' version (15.0) than being linked (<target>)` почти для
  каждого объектного файла `Shared.framework` — предупреждения полностью
  уходят на 15.0 (кроме одного не связанного с этим шума про
  `libicu.icudtl_dat.o`, который стабильно указывает 18.5 независимо от
  таргета и уже был на 18.2 без всяких проблем — не индикатор реального
  ограничения).
- Итого действующий минимум — **iOS 15.0**, а не 18.2. Понижение
  `IPHONEOS_DEPLOYMENT_TARGET` до 15.0 в обоих местах `project.pbxproj`
  расширяет поддержку с iPhone XS/XR+ до iPhone 6s/6s Plus/7/SE (2-го
  поколения)+.
- Проверено сборкой на реальном device-таргете (не только симулятор):
  `xcodebuild -destination 'generic/platform=iOS'` — `** BUILD SUCCEEDED **`
  для `arm64-apple-ios15.0`, без линкер-предупреждений.
- **Проверено на реальном железе с iOS 15 — пользователь запустил из Xcode
  на iPhone SE (iOS 15.8.8, build 19H422) без проблем.** Единственная
  оставшаяся оговорка — заголовки `Shared.framework` — plain Objective-C, без
  Swift-аннотаций `@available`, поэтому скрытая зависимость от API новее
  15.0 не поймалась бы статической проверкой доступности в Swift; живой
  прогон на устройстве — единственная реальная защита от этого, а не
  чтение кода.
- **Отдельно обнаружено: IntelliJ IDEA/Kotlin Multiplatform run configuration
  не умеет запускать сборку на физическом устройстве старше iOS 16** —
  жёсткая проверка самого плагина (`Error running 'Build': ... Please update
  the device at least to version 16`), не связанная с `IPHONEOS_DEPLOYMENT_TARGET`
  проекта или возможностями `Shared.framework`. Xcode этого ограничения не
  имеет — Run из Xcode на iPhone SE с iOS 15.8.8 работает штатно. Практический
  вывод: на устройствах <iOS 16 запуск делать через Xcode, IntelliJ — для
  редактирования кода и запуска Android/симулятора.

**Находки на карте прогулки (не агрегированной — `LiveTrackMap`, используется
и для «текущей прогулки», и для карты конкретной прогулки в Архиве) теперь
рисуются как фото гриба на маркере, а не сплошной цветной точкой.**
Агрегированная карта («Карта» → находки по всем прогулкам, `AggregatedFindsMap`)
осознанно не тронута — там источник кластеризованный (`GeoJsonOptions(cluster =
true)`), и одиночная картинка на кластер требует отдельного решения (обсуждали
отдельно: агрегация «преобладающего вида» через `clusterProperties`), не
делали.
- **Механизм — `SymbolLayer.iconImage`, а не `CircleLayer`.** Подтверждено по
  исходникам `maplibre-compose` 0.13.0 (в кэше Gradle, `-sources.jar`):
  `image(painter: Painter, size: DpSize, ...)` рисует произвольный Compose
  `Painter` в растровый bitmap и регистрирует его в стиле карты
  (реф-каунтинг, `ImageManager` — авто-регистрация/дерегистрация). Поэтому
  маркер — не сырое фото, а собственный `Painter`
  (`ui/map/MushroomMarkerIcon.kt`, `MushroomMarkerPainter`), рисующий вручную:
  чёрный круг (заливка) → белый круг поверх (оставляет чёрное кольцо по краю)
  → серый круг поверх (оставляет белое кольцо) → само фото гриба, обрезанное
  по кругу (`clipPath` + `addOval`) и вписанное как `ContentScale.Fit`
  (сравнение intrinsic-размера фото с диаметром внутреннего круга, аналогично
  тому, как `MushroomTile` на экране «Запись» вписывает то же самое фото).
  Итог — двойная окантовка (белая внутри, чёрная снаружи, обе по 2.dp — та же
  толщина, что уже была у обводки точки текущей геолокации) без обращения к
  `strokeColor`/`strokeWidth` (у `CircleLayer` только одна обводка, для двух
  цветов пришлось рисовать кольца руками).
- **Серый фон за фото — не константа, а `MaterialTheme.colorScheme.
  surfaceContainerHighest`**, взятый прямо в композиции (`rememberMushroomMarkerPainter`
  — обычный `@Composable`, вызывается внутри содержимого `MaplibreMap`, которое
  наследует тему приложения). Это ровно тот цвет, что использует `Card` по
  умолчанию (проверено по исходникам Material3 1.11.0-alpha07:
  `CardDefaults.cardColors()` → `FilledCardTokens.ContainerColor` →
  `ColorSchemeKeyTokens.SurfaceContainerHighest`) — то есть фон за прозрачными
  краями PNG на маркере совпадает с фоном за той же прозрачностью на плашке
  экрана «Запись» (см. Часть 6 про псевдо-прозрачность/flood fill), без
  дублирования хардкод-цвета.
- **Общий размер маркера — 32.dp** (константа `MUSHROOM_MARKER_SIZE`),
  подобран как компромисс: старая цветная точка находки была 12.dp
  (`radius = 6.dp`, без обводки) — с ней узнаваемое фото гриба физически не
  поместилось бы. 32.dp — наименьший размер, на котором сам гриб на фото ещё
  читаем, при этом заметно меньше маркера агрегированной карты. Отмечено
  пользователю как решение по ощущению, а не точный подбор — можно поменять
  по запросу.
- **`MapMarker` получил `iconRef: String?`** (`ui/map/LiveTrackMap.kt`) —
  раньше нёс только `colorHex`. Оба места, где строятся маркеры
  (`RecordMapScreen.kt`, `WalkMapScreen.kt`), теперь мапят `mark.categoryId`
  в целую `Category` (не только её цвет), потому что нужен и `colorHex`
  (фоллбэк), и `iconRef`. Находки без иконки (type=PHOTO/POI, они висят на
  служебной неактивной категории `category_misc` с `iconRef = null`, см.
  Часть 2) остаются старыми цветными точками (`CircleLayer`) — фото-маркер
  для них не имеет смысла.
- **Баг найден и исправлен во время живого тестирования: маркеры-находки
  иногда переставали отрисовываться вообще, без ошибок в логе.** Разобрано
  на эмуляторе (`Medium_Phone`): записал прогулку с находками двух видов
  грибов в одной точке (пока `currentLocation` был там же — маркер был виден,
  проглядывал из-под точки геолокации), затем сдвинул mock-GPS
  (`adb emu geo fix`) в другую точку — метка текущей геолокации переместилась,
  но маркеры находок в старой точке пропали полностью (не рисовались вообще,
  хотя точка была хорошо в границах видимой карты). Причина — оба цикла
  `markers.groupBy { ... }.forEach { ... }` (по `iconRef` для грибов, по
  `colorHex` для фото/POI) шли без `key()` вокруг тела; при изменении набора
  групп между рекомпозициями (например, во время того же теста добавился
  третий вид гриба — новая группа `iconRef`) Compose не гарантирует, что
  `rememberGeoJsonSource`/`SymbolLayer` внутри одной и той же позиции в
  `forEach` останутся привязаны к той же логической группе — слот мог
  переиспользоваться некорректно между вызовами. Тот же паттерн без `key()`
  уже был в коде ДО этой правки (когда всё рисовалось `CircleLayer` по
  `colorHex`) — но там баг был не так заметен (просто исчезновение сплошной
  точки труднее заметить/отличить от «находки нет на этом кадре», плюс раньше
  тестировали меньше сценариев с движущейся геолокацией). Исправлено
  оборачиванием обоих циклов в `key(iconRef) { ... }`/`key(colorHex) { ... }`
  — тот же приём, что уже использовался в `AggregatedFindsMap.kt` для
  `key(walkId)` по трекам прогулок.
- Проверено вживую на эмуляторе `Medium_Phone` через `adb emu geo fix` +
  прямое взаимодействие с UI (`adb shell input tap`): записаны находки трёх
  видов грибов (Porcini, Oyster mushroom, Bovine bolete) в двух разных точках
  (первая — Porcini+Oyster в одной точке, вторая — Bovine bolete в 111 м от
  первой), карта показывает оба маркера корректно с двойной окантовкой и
  фото гриба, видимым сквозь прозрачные края PNG на сером фоне; после фикса
  `key()` маркеры не пропадают при появлении новой группы видов и при
  движении текущей геолокации. Тестовые прогулки удалены через существующую
  функцию удаления в «Архиве» — архив и агрегированная карта пустые, как и до
  тестирования. `./gradlew :shared:compileAndroidMain
  :shared:compileKotlinIosArm64 :shared:compileKotlinIosSimulatorArm64
  :androidApp:assembleDebug` — чисто (те же 3 предсуществующих warning'а про
  `MapLibre Composable` в `MapScreen.kt`/`RecordMapScreen.kt`/
  `WalkMapScreen.kt`, не связанные с этой правкой).

---

## 8. Полезные команды

```bash
./gradlew build                      # полная сборка
./gradlew :shared:compileKotlin...   # быстрая проверка компиляции shared
./gradlew :androidApp:assembleDebug  # сборка Android APK
```

Логотип: исходник — карандашный рисунок головы лешего. По ТЗ стилизовать:
тёмно-зелёная голова на светлом фоне, слегка сглаженные линии, отдать как SVG.
